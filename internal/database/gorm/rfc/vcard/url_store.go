// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: protobuf/rfc6350/vcard/v1/contact.proto, protobuf/rfc6350/vcard/v1/contact_address.proto, protobuf/rfc6350/vcard/v1/contact_location.proto, protobuf/rfc6350/vcard/v1/contact_method.proto, protobuf/rfc6350/vcard/v1/contact_name.proto, protobuf/rfc6350/vcard/v1/contact_property.proto, protobuf/rfc6350/vcard/v1/contact_relation.proto, protobuf/rfc6350/vcard/v1/contact_speech.proto, protobuf/rfc6350/vcard/v1/date_or_text.proto, protobuf/rfc6350/vcard/v1/types.proto, protobuf/rfc6350/vcard/v1/vcard.proto
//
// database: rfc
// schema:   vcard
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package vcard

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// UrlStoreIface is the full data-access surface of Url: everything
// UrlStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedUrlStore struct {
//		UrlStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewUrlStore returns the concrete
// *UrlStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type UrlStoreIface interface {
	Create(ctx context.Context, m *Url) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Url, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Url) error
	GetByID(ctx context.Context, id string) (*Url, error)
	DeleteByID(ctx context.Context, id string) error
	ListByContactID(ctx context.Context, id string, opts gormx.ListOptions) ([]Url, error)
}

// UrlStore provides typed CRUD access to Url records.
// Url is the URL property, RFC 6350 section 6.7.8 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.7.8>. Reference: RFC 6350 "vCard Format Specification", section 6.7.8. https://www.rfc-editor.org/rfc/rfc6350.html#section-6.7.8
type UrlStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewUrlStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that UrlStore implements its own interface. Without it a
// signature could drift from UrlStoreIface and only break in whichever downstream
// package decorates it.
var _ UrlStoreIface = (*UrlStore)(nil)

// Compile-time proof that UrlStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Url] = (*UrlStore)(nil)

// NewUrlStore returns a UrlStore backed by db.
func NewUrlStore(db *gorm.DB) *UrlStore { return &UrlStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *UrlStore) WithTelemetry(t gormx.Telemetry) *UrlStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *UrlStore) Create(ctx context.Context, m *Url) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "vcard.Url/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "vcard.urls", "create", time.Since(start), err)
	return err
}

// List returns the Url records matching opts.
func (s *UrlStore) List(ctx context.Context, opts gormx.ListOptions) ([]Url, error) {
	var out []Url
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "vcard.Url/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "vcard.urls", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Url records matching opts.Where
// (pagination and ordering are ignored).
func (s *UrlStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "vcard.Url/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&Url{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "vcard.urls", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *UrlStore) Update(ctx context.Context, m *Url) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "vcard.Url/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "vcard.urls", "update", time.Since(start), err)
	return err
}

// GetByID fetches the Url with the given primary key.
func (s *UrlStore) GetByID(ctx context.Context, id string) (*Url, error) {
	var m Url
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "vcard.Url/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "vcard.urls", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Url with the given primary key.
func (s *UrlStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "vcard.Url/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&Url{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "vcard.urls", "delete", time.Since(start), err)
	return err
}

// ListByContactID returns the Url records whose contact_id matches id, with opts applied.
func (s *UrlStore) ListByContactID(ctx context.Context, id string, opts gormx.ListOptions) ([]Url, error) {
	var out []Url
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "vcard.Url/ListByContactID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("contact_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "vcard.urls", "list_by_contact_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}
