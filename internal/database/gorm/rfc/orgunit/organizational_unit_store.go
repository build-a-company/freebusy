// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: protobuf/rfc4519/organizational_unit/v1/organizational_unit.proto
//
// database: rfc
// schema:   orgunit
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package orgunit

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// OrganizationalUnitStoreIface is the full data-access surface of OrganizationalUnit: everything
// OrganizationalUnitStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedOrganizationalUnitStore struct {
//		OrganizationalUnitStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewOrganizationalUnitStore returns the concrete
// *OrganizationalUnitStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type OrganizationalUnitStoreIface interface {
	Create(ctx context.Context, m *OrganizationalUnit) error
	List(ctx context.Context, opts gormx.ListOptions) ([]OrganizationalUnit, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *OrganizationalUnit) error
	GetByID(ctx context.Context, id string) (*OrganizationalUnit, error)
	DeleteByID(ctx context.Context, id string) error
	GetByName(ctx context.Context, v string) (*OrganizationalUnit, error)
	GetByUID(ctx context.Context, v string) (*OrganizationalUnit, error)
}

// OrganizationalUnitStore provides typed CRUD access to OrganizationalUnit records.
// OrganizationalUnit is the LDAP 'organizationalUnit' object class, RFC 4519 section 3.11 <https://www.rfc-editor.org/rfc/rfc4519.html#section-3.11>. A flat resource, "organizationalUnits/{organizationalUnit}", not nested under Organization: section 3.11 defines MUST and MAY attributes and says nothing about DIT placement, so "organizations/{o}/organizationalUnits/{ou}" would assert a hierarchy the RFC does not require. Group faced the same question for its own optional 'o' reference and was built flat for the same reason -- this resource follows that precedent instead of inventing a new one. Attributes are repeated because LDAP attribute types are multi-valued by default. Section 3.11 lists twenty MAY attributes; the three modelled here -- description, businessCategory, seeAlso -- are the ones Organization already carries, kept identical rather than picking a different subset for two object classes whose optional attributes overlap this heavily. Reference: RFC 4519 "LDAP: Schema for User Applications", section 3.11. https://www.rfc-editor.org/rfc/rfc4519.html#section-3.11
type OrganizationalUnitStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewOrganizationalUnitStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that OrganizationalUnitStore implements its own interface. Without it a
// signature could drift from OrganizationalUnitStoreIface and only break in whichever downstream
// package decorates it.
var _ OrganizationalUnitStoreIface = (*OrganizationalUnitStore)(nil)

// Compile-time proof that OrganizationalUnitStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[OrganizationalUnit] = (*OrganizationalUnitStore)(nil)

// NewOrganizationalUnitStore returns a OrganizationalUnitStore backed by db.
func NewOrganizationalUnitStore(db *gorm.DB) *OrganizationalUnitStore {
	return &OrganizationalUnitStore{DB: db}
}

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *OrganizationalUnitStore) WithTelemetry(t gormx.Telemetry) *OrganizationalUnitStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *OrganizationalUnitStore) Create(ctx context.Context, m *OrganizationalUnit) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "orgunit.OrganizationalUnit/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "orgunit.organizational_units", "create", time.Since(start), err)
	return err
}

// List returns the OrganizationalUnit records matching opts.
func (s *OrganizationalUnitStore) List(ctx context.Context, opts gormx.ListOptions) ([]OrganizationalUnit, error) {
	var out []OrganizationalUnit
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "orgunit.OrganizationalUnit/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "orgunit.organizational_units", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of OrganizationalUnit records matching opts.Where
// (pagination and ordering are ignored).
func (s *OrganizationalUnitStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "orgunit.OrganizationalUnit/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&OrganizationalUnit{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "orgunit.organizational_units", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *OrganizationalUnitStore) Update(ctx context.Context, m *OrganizationalUnit) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "orgunit.OrganizationalUnit/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "orgunit.organizational_units", "update", time.Since(start), err)
	return err
}

// GetByID fetches the OrganizationalUnit with the given primary key.
func (s *OrganizationalUnitStore) GetByID(ctx context.Context, id string) (*OrganizationalUnit, error) {
	var m OrganizationalUnit
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "orgunit.OrganizationalUnit/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "orgunit.organizational_units", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the OrganizationalUnit with the given primary key.
func (s *OrganizationalUnitStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "orgunit.OrganizationalUnit/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&OrganizationalUnit{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "orgunit.organizational_units", "delete", time.Since(start), err)
	return err
}

// GetByName fetches the OrganizationalUnit with the given name (a unique column).
func (s *OrganizationalUnitStore) GetByName(ctx context.Context, v string) (*OrganizationalUnit, error) {
	var m OrganizationalUnit
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "orgunit.OrganizationalUnit/GetByName", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "name = ?", v).Error
	})
	tel.RecordOp(ctx, "orgunit.organizational_units", "get_by_name", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// GetByUID fetches the OrganizationalUnit with the given uid (a unique column).
func (s *OrganizationalUnitStore) GetByUID(ctx context.Context, v string) (*OrganizationalUnit, error) {
	var m OrganizationalUnit
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "orgunit.OrganizationalUnit/GetByUID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "uid = ?", v).Error
	})
	tel.RecordOp(ctx, "orgunit.organizational_units", "get_by_uid", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}
