// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: google/type/datetime.proto
//
// database: rfc
// schema:   common
//
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package common

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// TimeZoneStoreIface is the full data-access surface of TimeZone: everything
// TimeZoneStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedTimeZoneStore struct {
//		TimeZoneStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewTimeZoneStore returns the concrete
// *TimeZoneStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type TimeZoneStoreIface interface {
	Create(ctx context.Context, m *TimeZone) error
	List(ctx context.Context, opts gormx.ListOptions) ([]TimeZone, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *TimeZone) error
	GetByID(ctx context.Context, id string) (*TimeZone, error)
	DeleteByID(ctx context.Context, id string) error
}

// TimeZoneStore provides typed CRUD access to TimeZone records.
// Represents a time zone from the [IANA Time Zone Database](https://www.iana.org/time-zones).
type TimeZoneStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewTimeZoneStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that TimeZoneStore implements its own interface. Without it a
// signature could drift from TimeZoneStoreIface and only break in whichever downstream
// package decorates it.
var _ TimeZoneStoreIface = (*TimeZoneStore)(nil)

// Compile-time proof that TimeZoneStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[TimeZone] = (*TimeZoneStore)(nil)

// NewTimeZoneStore returns a TimeZoneStore backed by db.
func NewTimeZoneStore(db *gorm.DB) *TimeZoneStore { return &TimeZoneStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *TimeZoneStore) WithTelemetry(t gormx.Telemetry) *TimeZoneStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *TimeZoneStore) Create(ctx context.Context, m *TimeZone) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "common.TimeZone/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "common.time_zones", "create", time.Since(start), err)
	return err
}

// List returns the TimeZone records matching opts.
func (s *TimeZoneStore) List(ctx context.Context, opts gormx.ListOptions) ([]TimeZone, error) {
	var out []TimeZone
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "common.TimeZone/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "common.time_zones", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of TimeZone records matching opts.Where
// (pagination and ordering are ignored).
func (s *TimeZoneStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "common.TimeZone/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&TimeZone{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "common.time_zones", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *TimeZoneStore) Update(ctx context.Context, m *TimeZone) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "common.TimeZone/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "common.time_zones", "update", time.Since(start), err)
	return err
}

// GetByID fetches the TimeZone with the given primary key.
func (s *TimeZoneStore) GetByID(ctx context.Context, id string) (*TimeZone, error) {
	var m TimeZone
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "common.TimeZone/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "common.time_zones", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the TimeZone with the given primary key.
func (s *TimeZoneStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "common.TimeZone/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&TimeZone{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "common.time_zones", "delete", time.Since(start), err)
	return err
}
