// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.4-0.20260906073202-9760202f187b+dirty
// 	protoc (unknown)
// source: protobuf/rfc5545/event/v1/alarm.proto, protobuf/rfc5545/event/v1/event.proto, protobuf/rfc5545/event/v1/links.proto, protobuf/rfc5545/event/v1/participant.proto, protobuf/rfc5545/event/v1/publishing.proto, protobuf/rfc5545/event/v1/publishing_types.proto, protobuf/rfc5545/event/v1/recurrence.proto, protobuf/rfc5545/event/v1/recurrence_parts.proto, protobuf/rfc5545/event/v1/types.proto
//
// database: rfc
// schema:   event
//
// annotations: entity.v1 v1.5.4-0.20260906073202-9760202f187b+dirty, store.v1 v1.5.4-0.20260906073202-9760202f187b+dirty
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package event

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// OrganizerStoreIface is the full data-access surface of Organizer: everything
// OrganizerStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedOrganizerStore struct {
//		OrganizerStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewOrganizerStore returns the concrete
// *OrganizerStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type OrganizerStoreIface interface {
	Create(ctx context.Context, m *Organizer) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Organizer, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Organizer) error
	GetByID(ctx context.Context, id string) (*Organizer, error)
	DeleteByID(ctx context.Context, id string) error
}

// OrganizerStore provides typed CRUD access to Organizer records.
// Organizer is the ORGANIZER property, RFC 5545 section 3.8.4.3 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.8.4.3>. Distinct from an Attendee with ROLE=CHAIR: the organizer owns the event and is who iTIP replies are addressed to, which is a scheduling fact rather than a statement about who runs the meeting. Reference: RFC 5545 "Internet Calendaring and Scheduling Core Object Specification (iCalendar)", section 3.8.4.3. https://www.rfc-editor.org/rfc/rfc5545.html#section-3.8.4.3
type OrganizerStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewOrganizerStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that OrganizerStore implements its own interface. Without it a
// signature could drift from OrganizerStoreIface and only break in whichever downstream
// package decorates it.
var _ OrganizerStoreIface = (*OrganizerStore)(nil)

// Compile-time proof that OrganizerStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Organizer] = (*OrganizerStore)(nil)

// NewOrganizerStore returns a OrganizerStore backed by db.
func NewOrganizerStore(db *gorm.DB) *OrganizerStore { return &OrganizerStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *OrganizerStore) WithTelemetry(t gormx.Telemetry) *OrganizerStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *OrganizerStore) Create(ctx context.Context, m *Organizer) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "event.Organizer/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "event.organizers", "create", time.Since(start), err)
	return err
}

// List returns the Organizer records matching opts.
func (s *OrganizerStore) List(ctx context.Context, opts gormx.ListOptions) ([]Organizer, error) {
	var out []Organizer
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "event.Organizer/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "event.organizers", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Organizer records matching opts.Where
// (pagination and ordering are ignored).
func (s *OrganizerStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "event.Organizer/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&Organizer{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "event.organizers", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *OrganizerStore) Update(ctx context.Context, m *Organizer) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "event.Organizer/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "event.organizers", "update", time.Since(start), err)
	return err
}

// GetByID fetches the Organizer with the given primary key.
func (s *OrganizerStore) GetByID(ctx context.Context, id string) (*Organizer, error) {
	var m Organizer
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "event.Organizer/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "event.organizers", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Organizer with the given primary key.
func (s *OrganizerStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "event.Organizer/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&Organizer{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "event.organizers", "delete", time.Since(start), err)
	return err
}
