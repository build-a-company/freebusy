// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.4-0.20260906073202-9760202f187b+dirty
// 	protoc (unknown)
// source: protobuf/rfc9553/card/v1/card.proto, protobuf/rfc9553/card/v1/name.proto, protobuf/rfc9553/card/v1/speech.proto
//
// database: rfc
// schema:   card
//
// annotations: entity.v1 v1.5.4-0.20260906073202-9760202f187b+dirty, store.v1 v1.5.4-0.20260906073202-9760202f187b+dirty
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package card

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// SpeechStoreIface is the full data-access surface of Speech: everything
// SpeechStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedSpeechStore struct {
//		SpeechStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewSpeechStore returns the concrete
// *SpeechStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type SpeechStoreIface interface {
	Create(ctx context.Context, m *Speech) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Speech, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Speech) error
	GetByID(ctx context.Context, id string) (*Speech, error)
	DeleteByID(ctx context.Context, id string) error
}

// SpeechStore provides typed CRUD access to Speech records.
// Speech is the Card "speakToAs" property, RFC 9553 section 2.2.4 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.4>. How to refer to the contact in speech and prose. Named `Speech` rather than the RFC's `SpeakToAs`: AIP-140 <https://aip.dev/140> bans prepositions in field names, so the field cannot be `speak_to_as`, and a message whose name does not match its field reads worse than one renamed honestly. Section 2.2.4 is explicit that grammatical gender "does not allow inferring the gender identities or assigned sex of the contact" -- it is a property of the language being written, and `pronouns` is the separate, self-reported thing. The two are deliberately distinct fields here for that reason. Reference: RFC 9553 "JSContact: A JSON Representation of Contact Data", section 2.2.4. https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.4
type SpeechStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewSpeechStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that SpeechStore implements its own interface. Without it a
// signature could drift from SpeechStoreIface and only break in whichever downstream
// package decorates it.
var _ SpeechStoreIface = (*SpeechStore)(nil)

// Compile-time proof that SpeechStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Speech] = (*SpeechStore)(nil)

// NewSpeechStore returns a SpeechStore backed by db.
func NewSpeechStore(db *gorm.DB) *SpeechStore { return &SpeechStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *SpeechStore) WithTelemetry(t gormx.Telemetry) *SpeechStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *SpeechStore) Create(ctx context.Context, m *Speech) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "card.Speech/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "card.speeches", "create", time.Since(start), err)
	return err
}

// List returns the Speech records matching opts.
func (s *SpeechStore) List(ctx context.Context, opts gormx.ListOptions) ([]Speech, error) {
	var out []Speech
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "card.Speech/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "card.speeches", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Speech records matching opts.Where
// (pagination and ordering are ignored).
func (s *SpeechStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "card.Speech/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&Speech{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "card.speeches", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *SpeechStore) Update(ctx context.Context, m *Speech) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "card.Speech/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "card.speeches", "update", time.Since(start), err)
	return err
}

// GetByID fetches the Speech with the given primary key.
func (s *SpeechStore) GetByID(ctx context.Context, id string) (*Speech, error) {
	var m Speech
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "card.Speech/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "card.speeches", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Speech with the given primary key.
func (s *SpeechStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "card.Speech/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&Speech{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "card.speeches", "delete", time.Since(start), err)
	return err
}
