// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: freebusy/promocode/v1/promocode.proto
//
// database: freebusy
// schema:   promocode
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package promocode

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// RedemptionWindowStoreIface is the full data-access surface of RedemptionWindow: everything
// RedemptionWindowStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedRedemptionWindowStore struct {
//		RedemptionWindowStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewRedemptionWindowStore returns the concrete
// *RedemptionWindowStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type RedemptionWindowStoreIface interface {
	Create(ctx context.Context, m *RedemptionWindow) error
	List(ctx context.Context, opts gormx.ListOptions) ([]RedemptionWindow, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *RedemptionWindow) error
	GetByID(ctx context.Context, id string) (*RedemptionWindow, error)
	DeleteByID(ctx context.Context, id string) error
}

// RedemptionWindowStore provides typed CRUD access to RedemptionWindow records.
// RedemptionWindow bounds when a code can be redeemed; an unset bound is open-ended. Nested value object → belongs-to promocode.redemption_windows.
type RedemptionWindowStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewRedemptionWindowStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that RedemptionWindowStore implements its own interface. Without it a
// signature could drift from RedemptionWindowStoreIface and only break in whichever downstream
// package decorates it.
var _ RedemptionWindowStoreIface = (*RedemptionWindowStore)(nil)

// Compile-time proof that RedemptionWindowStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[RedemptionWindow] = (*RedemptionWindowStore)(nil)

// NewRedemptionWindowStore returns a RedemptionWindowStore backed by db.
func NewRedemptionWindowStore(db *gorm.DB) *RedemptionWindowStore {
	return &RedemptionWindowStore{DB: db}
}

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *RedemptionWindowStore) WithTelemetry(t gormx.Telemetry) *RedemptionWindowStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *RedemptionWindowStore) Create(ctx context.Context, m *RedemptionWindow) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "promocode.RedemptionWindow/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "promocode.redemption_windows", "create", time.Since(start), err)
	return err
}

// List returns the RedemptionWindow records matching opts.
func (s *RedemptionWindowStore) List(ctx context.Context, opts gormx.ListOptions) ([]RedemptionWindow, error) {
	var out []RedemptionWindow
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "promocode.RedemptionWindow/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "promocode.redemption_windows", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of RedemptionWindow records matching opts.Where
// (pagination and ordering are ignored).
func (s *RedemptionWindowStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "promocode.RedemptionWindow/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&RedemptionWindow{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "promocode.redemption_windows", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *RedemptionWindowStore) Update(ctx context.Context, m *RedemptionWindow) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "promocode.RedemptionWindow/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "promocode.redemption_windows", "update", time.Since(start), err)
	return err
}

// GetByID fetches the RedemptionWindow with the given primary key.
func (s *RedemptionWindowStore) GetByID(ctx context.Context, id string) (*RedemptionWindow, error) {
	var m RedemptionWindow
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "promocode.RedemptionWindow/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "promocode.redemption_windows", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the RedemptionWindow with the given primary key.
func (s *RedemptionWindowStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "promocode.RedemptionWindow/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&RedemptionWindow{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "promocode.redemption_windows", "delete", time.Since(start), err)
	return err
}
