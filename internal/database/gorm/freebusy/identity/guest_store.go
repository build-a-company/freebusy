// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.1
// 	protoc (unknown)
// source: freebusy/identity/v1/guest.proto, freebusy/identity/v1/identity.proto
//
// database: freebusy
// schema:   identity
//
// annotations: entity.v1 v1.5.1, store.v1 v1.5.1
// engine:      protokit v1.2.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package identity

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// GuestStoreIface is the full data-access surface of Guest: everything
// GuestStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedGuestStore struct {
//		GuestStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewGuestStore returns the concrete
// *GuestStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type GuestStoreIface interface {
	Create(ctx context.Context, m *Guest) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Guest, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Guest) error
	GetByID(ctx context.Context, id string) (*Guest, error)
	DeleteByID(ctx context.Context, id string) error
	ListByBookingID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error)
	ListByIDDocumentID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error)
	ListByPermanentAddressID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error)
	ListByLocalAddressID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error)
	ListByForeignerID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error)
	ListByPreferencesID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error)
}

// GuestStore provides typed CRUD access to Guest records.
// A guest is a person who stays under a booking. It is one of three distinct people the system models, all in the identity domain: - User   (identity.proto): the account that signs in and books online. - Guest  (this message):   a person actually staying — the party on a booking. - Member (organisation):   hotel staff who manage the chain/property. The booker (a User, or an anonymous contact) is not necessarily a guest, and a booking has one or more guests. This message captures what a hotel records on a Guest Registration Card at check-in — identity, nationality, and ID — plus the foreigner-registration details required for foreign nationals (e.g. India's Form C / FRRO), and the guest's own stay preferences. It is an embedded value on a booking, not an addressable account resource.
type GuestStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewGuestStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that GuestStore implements its own interface. Without it a
// signature could drift from GuestStoreIface and only break in whichever downstream
// package decorates it.
var _ GuestStoreIface = (*GuestStore)(nil)

// Compile-time proof that GuestStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Guest] = (*GuestStore)(nil)

// NewGuestStore returns a GuestStore backed by db.
func NewGuestStore(db *gorm.DB) *GuestStore { return &GuestStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *GuestStore) WithTelemetry(t gormx.Telemetry) *GuestStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *GuestStore) Create(ctx context.Context, m *Guest) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "identity.guests", "create", time.Since(start), err)
	return err
}

// List returns the Guest records matching opts.
func (s *GuestStore) List(ctx context.Context, opts gormx.ListOptions) ([]Guest, error) {
	var out []Guest
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "identity.guests", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Guest records matching opts.Where
// (pagination and ordering are ignored).
func (s *GuestStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&Guest{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "identity.guests", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *GuestStore) Update(ctx context.Context, m *Guest) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "identity.guests", "update", time.Since(start), err)
	return err
}

// GetByID fetches the Guest with the given primary key.
func (s *GuestStore) GetByID(ctx context.Context, id string) (*Guest, error) {
	var m Guest
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "identity.guests", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Guest with the given primary key.
func (s *GuestStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&Guest{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "identity.guests", "delete", time.Since(start), err)
	return err
}

// ListByBookingID returns the Guest records whose booking_id matches id, with opts applied.
func (s *GuestStore) ListByBookingID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error) {
	var out []Guest
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/ListByBookingID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("booking_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "identity.guests", "list_by_booking_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ListByIDDocumentID returns the Guest records whose id_document_id matches id, with opts applied.
func (s *GuestStore) ListByIDDocumentID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error) {
	var out []Guest
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/ListByIDDocumentID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("id_document_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "identity.guests", "list_by_id_document_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ListByPermanentAddressID returns the Guest records whose permanent_address_id matches id, with opts applied.
func (s *GuestStore) ListByPermanentAddressID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error) {
	var out []Guest
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/ListByPermanentAddressID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("permanent_address_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "identity.guests", "list_by_permanent_address_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ListByLocalAddressID returns the Guest records whose local_address_id matches id, with opts applied.
func (s *GuestStore) ListByLocalAddressID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error) {
	var out []Guest
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/ListByLocalAddressID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("local_address_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "identity.guests", "list_by_local_address_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ListByForeignerID returns the Guest records whose foreigner_id matches id, with opts applied.
func (s *GuestStore) ListByForeignerID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error) {
	var out []Guest
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/ListByForeignerID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("foreigner_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "identity.guests", "list_by_foreigner_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ListByPreferencesID returns the Guest records whose preferences_id matches id, with opts applied.
func (s *GuestStore) ListByPreferencesID(ctx context.Context, id string, opts gormx.ListOptions) ([]Guest, error) {
	var out []Guest
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.Guest/ListByPreferencesID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("preferences_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "identity.guests", "list_by_preferences_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}
