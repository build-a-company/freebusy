// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.1
// 	protoc (unknown)
// source: freebusy/identity/v1/guest.proto, freebusy/identity/v1/identity.proto
//
// database: freebusy
// schema:   identity
//
// annotations: entity.v1 v1.5.1, store.v1 v1.5.1
// engine:      protokit v1.2.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package identity

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// GuestPreferencesStoreIface is the full data-access surface of GuestPreferences: everything
// GuestPreferencesStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedGuestPreferencesStore struct {
//		GuestPreferencesStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewGuestPreferencesStore returns the concrete
// *GuestPreferencesStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type GuestPreferencesStoreIface interface {
	Create(ctx context.Context, m *GuestPreferences) error
	List(ctx context.Context, opts gormx.ListOptions) ([]GuestPreferences, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *GuestPreferences) error
	GetByID(ctx context.Context, id string) (*GuestPreferences, error)
	DeleteByID(ctx context.Context, id string) error
}

// GuestPreferencesStore provides typed CRUD access to GuestPreferences records.
// A guest's stay preferences and special requests. All optional; used to guide unit assignment and to surface requests to housekeeping / front desk.
type GuestPreferencesStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewGuestPreferencesStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that GuestPreferencesStore implements its own interface. Without it a
// signature could drift from GuestPreferencesStoreIface and only break in whichever downstream
// package decorates it.
var _ GuestPreferencesStoreIface = (*GuestPreferencesStore)(nil)

// Compile-time proof that GuestPreferencesStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[GuestPreferences] = (*GuestPreferencesStore)(nil)

// NewGuestPreferencesStore returns a GuestPreferencesStore backed by db.
func NewGuestPreferencesStore(db *gorm.DB) *GuestPreferencesStore {
	return &GuestPreferencesStore{DB: db}
}

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *GuestPreferencesStore) WithTelemetry(t gormx.Telemetry) *GuestPreferencesStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *GuestPreferencesStore) Create(ctx context.Context, m *GuestPreferences) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.GuestPreferences/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "identity.guest_preferences", "create", time.Since(start), err)
	return err
}

// List returns the GuestPreferences records matching opts.
func (s *GuestPreferencesStore) List(ctx context.Context, opts gormx.ListOptions) ([]GuestPreferences, error) {
	var out []GuestPreferences
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.GuestPreferences/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "identity.guest_preferences", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of GuestPreferences records matching opts.Where
// (pagination and ordering are ignored).
func (s *GuestPreferencesStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.GuestPreferences/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&GuestPreferences{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "identity.guest_preferences", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *GuestPreferencesStore) Update(ctx context.Context, m *GuestPreferences) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.GuestPreferences/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "identity.guest_preferences", "update", time.Since(start), err)
	return err
}

// GetByID fetches the GuestPreferences with the given primary key.
func (s *GuestPreferencesStore) GetByID(ctx context.Context, id string) (*GuestPreferences, error) {
	var m GuestPreferences
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.GuestPreferences/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "identity.guest_preferences", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the GuestPreferences with the given primary key.
func (s *GuestPreferencesStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "identity.GuestPreferences/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&GuestPreferences{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "identity.guest_preferences", "delete", time.Since(start), err)
	return err
}
