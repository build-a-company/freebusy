// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.1
// 	protoc (unknown)
// source: freebusy/channel/v1/channel.proto, freebusy/channel/v1/channel_messages.proto
//
// database: freebusy
// schema:   channel
//
// annotations: entity.v1 v1.5.1, store.v1 v1.5.1
// engine:      protokit v1.2.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package channel

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// ChannelStoreIface is the full data-access surface of Channel: everything
// ChannelStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedChannelStore struct {
//		ChannelStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewChannelStore returns the concrete
// *ChannelStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type ChannelStoreIface interface {
	Create(ctx context.Context, m *Channel) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Channel, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Channel) error
	GetByID(ctx context.Context, id string) (*Channel, error)
	DeleteByID(ctx context.Context, id string) error
	GetByName(ctx context.Context, v string) (*Channel, error)
}

// ChannelStore provides typed CRUD access to Channel records.
// A connection between one property and one distribution channel (OTA/GDS). It is the anchor for 2-way ARI: availability/rates/inventory are pushed out per mapped unit, and reservations made on the channel are pulled in as bookings. Credentials are never carried in the API — only an opaque handle to where they are stored.
type ChannelStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewChannelStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that ChannelStore implements its own interface. Without it a
// signature could drift from ChannelStoreIface and only break in whichever downstream
// package decorates it.
var _ ChannelStoreIface = (*ChannelStore)(nil)

// Compile-time proof that ChannelStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Channel] = (*ChannelStore)(nil)

// NewChannelStore returns a ChannelStore backed by db.
func NewChannelStore(db *gorm.DB) *ChannelStore { return &ChannelStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *ChannelStore) WithTelemetry(t gormx.Telemetry) *ChannelStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *ChannelStore) Create(ctx context.Context, m *Channel) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "channel.Channel/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "channel.resource", "create", time.Since(start), err)
	return err
}

// List returns the Channel records matching opts.
func (s *ChannelStore) List(ctx context.Context, opts gormx.ListOptions) ([]Channel, error) {
	var out []Channel
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "channel.Channel/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "channel.resource", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Channel records matching opts.Where
// (pagination and ordering are ignored).
func (s *ChannelStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "channel.Channel/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&Channel{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "channel.resource", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *ChannelStore) Update(ctx context.Context, m *Channel) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "channel.Channel/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "channel.resource", "update", time.Since(start), err)
	return err
}

// GetByID fetches the Channel with the given primary key.
func (s *ChannelStore) GetByID(ctx context.Context, id string) (*Channel, error) {
	var m Channel
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "channel.Channel/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "channel.resource", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Channel with the given primary key.
func (s *ChannelStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "channel.Channel/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&Channel{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "channel.resource", "delete", time.Since(start), err)
	return err
}

// GetByName fetches the Channel with the given name (a unique column).
func (s *ChannelStore) GetByName(ctx context.Context, v string) (*Channel, error) {
	var m Channel
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "channel.Channel/GetByName", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "name = ?", v).Error
	})
	tel.RecordOp(ctx, "channel.resource", "get_by_name", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}
