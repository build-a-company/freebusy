// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: freebusy/pricing/v1/pricing.proto
//
// database: freebusy
// schema:   pricing
//
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

package pricing

import (
	"fmt"
	"strings"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/freebusy/common"
	"github.com/oh-tarnished/freebusy/protobuf/generated/go/pricing/v1/pricingpbv1"
	"google.golang.org/genproto/googleapis/type/date"
	"google.golang.org/genproto/googleapis/type/dayofweek"
	"google.golang.org/protobuf/types/known/timestamppb"
)

// PricingUnitFromProto maps the proto enum onto its stored form ("" for
// unspecified or unknown values, so the column is omitted / left to default).
func PricingUnitFromProto(v pricingpbv1.PricingUnit) PricingUnit {
	switch v {
	case pricingpbv1.PricingUnit_PRICING_UNIT_PER_BOOKING:
		return PricingUnitPerBooking
	case pricingpbv1.PricingUnit_PRICING_UNIT_PER_NIGHT:
		return PricingUnitPerNight
	case pricingpbv1.PricingUnit_PRICING_UNIT_PER_PERSON:
		return PricingUnitPerPerson
	}
	return ""
}

// PricingUnitToProto maps the stored form back onto the proto enum
// (unspecified for empty or unknown values).
func PricingUnitToProto(v PricingUnit) pricingpbv1.PricingUnit {
	switch v {
	case PricingUnitPerBooking:
		return pricingpbv1.PricingUnit_PRICING_UNIT_PER_BOOKING
	case PricingUnitPerNight:
		return pricingpbv1.PricingUnit_PRICING_UNIT_PER_NIGHT
	case PricingUnitPerPerson:
		return pricingpbv1.PricingUnit_PRICING_UNIT_PER_PERSON
	}
	return 0
}

// PricingUnitPtrFromProto is PricingUnitFromProto for nullable columns:
// unspecified maps to nil.
func PricingUnitPtrFromProto(v pricingpbv1.PricingUnit) *PricingUnit {
	if s := PricingUnitFromProto(v); s != "" {
		return &s
	}
	return nil
}

// PricingUnitPtrToProto is PricingUnitToProto for nullable columns.
func PricingUnitPtrToProto(v *PricingUnit) pricingpbv1.PricingUnit {
	if v == nil {
		return 0
	}
	return PricingUnitToProto(*v)
}

// RatePlanStateFromProto maps the proto enum onto its stored form ("" for
// unspecified or unknown values, so the column is omitted / left to default).
func RatePlanStateFromProto(v pricingpbv1.RatePlanState) RatePlanState {
	switch v {
	case pricingpbv1.RatePlanState_RATE_PLAN_STATE_ACTIVE:
		return RatePlanStateActive
	case pricingpbv1.RatePlanState_RATE_PLAN_STATE_ARCHIVED:
		return RatePlanStateArchived
	}
	return ""
}

// RatePlanStateToProto maps the stored form back onto the proto enum
// (unspecified for empty or unknown values).
func RatePlanStateToProto(v RatePlanState) pricingpbv1.RatePlanState {
	switch v {
	case RatePlanStateActive:
		return pricingpbv1.RatePlanState_RATE_PLAN_STATE_ACTIVE
	case RatePlanStateArchived:
		return pricingpbv1.RatePlanState_RATE_PLAN_STATE_ARCHIVED
	}
	return 0
}

// RatePlanStatePtrFromProto is RatePlanStateFromProto for nullable columns:
// unspecified maps to nil.
func RatePlanStatePtrFromProto(v pricingpbv1.RatePlanState) *RatePlanState {
	if s := RatePlanStateFromProto(v); s != "" {
		return &s
	}
	return nil
}

// RatePlanStatePtrToProto is RatePlanStateToProto for nullable columns.
func RatePlanStatePtrToProto(v *RatePlanState) pricingpbv1.RatePlanState {
	if v == nil {
		return 0
	}
	return RatePlanStateToProto(*v)
}

// DynamicTriggerFromProto maps the proto enum onto its stored form ("" for
// unspecified or unknown values, so the column is omitted / left to default).
func DynamicTriggerFromProto(v pricingpbv1.DynamicTrigger) DynamicTrigger {
	switch v {
	case pricingpbv1.DynamicTrigger_DYNAMIC_TRIGGER_OCCUPANCY:
		return DynamicTriggerOccupancy
	case pricingpbv1.DynamicTrigger_DYNAMIC_TRIGGER_LEAD_TIME:
		return DynamicTriggerLeadTime
	case pricingpbv1.DynamicTrigger_DYNAMIC_TRIGGER_LENGTH_OF_STAY:
		return DynamicTriggerLengthOfStay
	}
	return ""
}

// DynamicTriggerToProto maps the stored form back onto the proto enum
// (unspecified for empty or unknown values).
func DynamicTriggerToProto(v DynamicTrigger) pricingpbv1.DynamicTrigger {
	switch v {
	case DynamicTriggerOccupancy:
		return pricingpbv1.DynamicTrigger_DYNAMIC_TRIGGER_OCCUPANCY
	case DynamicTriggerLeadTime:
		return pricingpbv1.DynamicTrigger_DYNAMIC_TRIGGER_LEAD_TIME
	case DynamicTriggerLengthOfStay:
		return pricingpbv1.DynamicTrigger_DYNAMIC_TRIGGER_LENGTH_OF_STAY
	}
	return 0
}

// DynamicTriggerPtrFromProto is DynamicTriggerFromProto for nullable columns:
// unspecified maps to nil.
func DynamicTriggerPtrFromProto(v pricingpbv1.DynamicTrigger) *DynamicTrigger {
	if s := DynamicTriggerFromProto(v); s != "" {
		return &s
	}
	return nil
}

// DynamicTriggerPtrToProto is DynamicTriggerToProto for nullable columns.
func DynamicTriggerPtrToProto(v *DynamicTrigger) pricingpbv1.DynamicTrigger {
	if v == nil {
		return 0
	}
	return DynamicTriggerToProto(*v)
}

// RatePlanToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func RatePlanToProto(m *RatePlan) *pricingpbv1.RatePlan {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.RatePlan{}
	out.Name = m.Name
	out.Resource = m.Resource
	out.DisplayName = m.DisplayName
	out.Description = fromPtr(m.Description)
	out.PricingUnit = PricingUnitPtrToProto(m.PricingUnit)
	out.State = RatePlanStatePtrToProto(m.State)
	out.CreateTime = goValToTs(m.CreateTime)
	out.UpdateTime = goValToTs(m.UpdateTime)
	out.Etag = fromPtr(m.Etag)
	out.Price = common.MoneyToProto(m.Price)
	out.Bounds = PriceBoundsToProto(m.Bounds)
	return out
}

// RatePlanFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func RatePlanFromProto(pb *pricingpbv1.RatePlan) *RatePlan {
	if pb == nil {
		return nil
	}
	m := &RatePlan{}
	// not mapped here: price (sub-row graph), bounds (sub-row graph)
	m.Name = pb.GetName()
	m.Resource = pb.GetResource()
	m.DisplayName = pb.GetDisplayName()
	m.Description = toPtr(pb.GetDescription())
	m.PricingUnit = PricingUnitPtrFromProto(pb.GetPricingUnit())
	m.State = RatePlanStatePtrFromProto(pb.GetState())
	m.Etag = toPtr(pb.GetEtag())
	return m
}

// RateOverrideToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func RateOverrideToProto(m *RateOverride) *pricingpbv1.RateOverride {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.RateOverride{}
	out.Weekdays = enumsFromCSV[dayofweek.DayOfWeek](m.Weekdays, dayofweek.DayOfWeek_value)
	out.DateRange = DateRangeToProto(m.DateRange)
	out.Price = common.MoneyToProto(m.Price)
	return out
}

// RateOverrideFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func RateOverrideFromProto(pb *pricingpbv1.RateOverride) *RateOverride {
	if pb == nil {
		return nil
	}
	m := &RateOverride{}
	// not mapped here: date_range (sub-row graph), price (sub-row graph)
	m.Weekdays = enumsToCSV(pb.GetWeekdays())
	return m
}

// LosDiscountToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func LosDiscountToProto(m *LosDiscount) *pricingpbv1.LosDiscount {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.LosDiscount{}
	out.MinNights = m.MinNights
	out.PercentOff = fromPtr(m.PercentOff)
	out.AmountOff = common.MoneyToProto(m.AmountOff)
	return out
}

// LosDiscountFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func LosDiscountFromProto(pb *pricingpbv1.LosDiscount) *LosDiscount {
	if pb == nil {
		return nil
	}
	m := &LosDiscount{}
	// not mapped here: amount_off (sub-row graph)
	m.MinNights = pb.GetMinNights()
	m.PercentOff = toPtr(pb.GetPercentOff())
	return m
}

// FeeToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func FeeToProto(m *Fee) *pricingpbv1.Fee {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.Fee{}
	out.Code = m.Code
	out.DisplayName = fromPtr(m.DisplayName)
	out.Percent = fromPtr(m.Percent)
	out.PricingUnit = PricingUnitPtrToProto(m.PricingUnit)
	out.Taxable = fromPtr(m.Taxable)
	out.Amount = common.MoneyToProto(m.Amount)
	return out
}

// FeeFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func FeeFromProto(pb *pricingpbv1.Fee) *Fee {
	if pb == nil {
		return nil
	}
	m := &Fee{}
	// not mapped here: amount (sub-row graph)
	m.Code = pb.GetCode()
	m.DisplayName = toPtr(pb.GetDisplayName())
	m.Percent = toPtr(pb.GetPercent())
	m.PricingUnit = PricingUnitPtrFromProto(pb.GetPricingUnit())
	m.Taxable = toPtr(pb.GetTaxable())
	return m
}

// TaxToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func TaxToProto(m *Tax) *pricingpbv1.Tax {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.Tax{}
	out.Code = m.Code
	out.DisplayName = fromPtr(m.DisplayName)
	out.Percent = m.Percent
	return out
}

// TaxFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func TaxFromProto(pb *pricingpbv1.Tax) *Tax {
	if pb == nil {
		return nil
	}
	m := &Tax{}
	m.Code = pb.GetCode()
	m.DisplayName = toPtr(pb.GetDisplayName())
	m.Percent = pb.GetPercent()
	return m
}

// DynamicRuleToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func DynamicRuleToProto(m *DynamicRule) *pricingpbv1.DynamicRule {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.DynamicRule{}
	out.Trigger = DynamicTriggerToProto(m.Trigger)
	return out
}

// DynamicRuleFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func DynamicRuleFromProto(pb *pricingpbv1.DynamicRule) *DynamicRule {
	if pb == nil {
		return nil
	}
	m := &DynamicRule{}
	m.Trigger = DynamicTriggerFromProto(pb.GetTrigger())
	return m
}

// PriceBoundsToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func PriceBoundsToProto(m *PriceBounds) *pricingpbv1.PriceBounds {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.PriceBounds{}
	out.Floor = common.MoneyToProto(m.Floor)
	out.Ceiling = common.MoneyToProto(m.Ceiling)
	return out
}

// PriceBoundsFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func PriceBoundsFromProto(pb *pricingpbv1.PriceBounds) *PriceBounds {
	if pb == nil {
		return nil
	}
	m := &PriceBounds{}
	// not mapped here: floor (sub-row graph), ceiling (sub-row graph)
	return m
}

// DateRangeToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func DateRangeToProto(m *DateRange) *pricingpbv1.DateRange {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.DateRange{}
	out.StartDate = goValToDate(m.StartDate)
	out.EndDate = goValToDate(m.EndDate)
	return out
}

// DateRangeFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func DateRangeFromProto(pb *pricingpbv1.DateRange) *DateRange {
	if pb == nil {
		return nil
	}
	m := &DateRange{}
	m.StartDate = dateToGoVal(pb.GetStartDate())
	m.EndDate = dateToGoVal(pb.GetEndDate())
	return m
}

// DynamicBandToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func DynamicBandToProto(m *DynamicBand) *pricingpbv1.DynamicBand {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.DynamicBand{}
	out.MinThreshold = m.MinThreshold
	out.MaxThreshold = fromPtr(m.MaxThreshold)
	out.DisplayName = fromPtr(m.DisplayName)
	out.Adjustment = AdjustmentToProto(m.Adjustment)
	return out
}

// DynamicBandFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func DynamicBandFromProto(pb *pricingpbv1.DynamicBand) *DynamicBand {
	if pb == nil {
		return nil
	}
	m := &DynamicBand{}
	// not mapped here: adjustment (sub-row graph)
	m.MinThreshold = pb.GetMinThreshold()
	m.MaxThreshold = toPtr(pb.GetMaxThreshold())
	m.DisplayName = toPtr(pb.GetDisplayName())
	return m
}

// AdjustmentToProto converts the stored row (with its belongs-to associations,
// when preloaded) to its proto message. Nil in, nil out.
func AdjustmentToProto(m *Adjustment) *pricingpbv1.Adjustment {
	if m == nil {
		return nil
	}
	out := &pricingpbv1.Adjustment{}
	out.PercentDelta = fromPtr(m.PercentDelta)
	out.AmountDelta = common.MoneyToProto(m.AmountDelta)
	return out
}

// AdjustmentFromProto maps the proto message's data fields onto a fresh row.
// It never invents keys or relations: primary keys, resource references, and
// sub-row graph wiring stay with the caller. Nil in, nil out.
func AdjustmentFromProto(pb *pricingpbv1.Adjustment) *Adjustment {
	if pb == nil {
		return nil
	}
	m := &Adjustment{}
	// not mapped here: amount_delta (sub-row graph)
	m.PercentDelta = toPtr(pb.GetPercentDelta())
	return m
}

// toPtr returns a pointer to v, or nil for the zero value — a nullable column
// stores absence, not the proto zero.
func toPtr[T comparable](v T) *T {
	var zero T
	if v == zero {
		return nil
	}
	return &v
}

// fromPtr dereferences p, returning the zero value for nil.
func fromPtr[T any](p *T) T {
	if p == nil {
		var zero T
		return zero
	}
	return *p
}

// tsToGo maps a proto timestamp onto a nullable TIMESTAMPTZ value.
func tsToGo(ts *timestamppb.Timestamp) *time.Time {
	if ts == nil {
		return nil
	}
	t := ts.AsTime()
	return &t
}

// tsToGoVal maps a proto timestamp onto a NOT NULL TIMESTAMPTZ value.
func tsToGoVal(ts *timestamppb.Timestamp) time.Time {
	if ts == nil {
		return time.Time{}
	}
	return ts.AsTime()
}

// goToTs maps a nullable TIMESTAMPTZ value back onto a proto timestamp.
func goToTs(t *time.Time) *timestamppb.Timestamp {
	if t == nil || t.IsZero() {
		return nil
	}
	return timestamppb.New(*t)
}

// goValToTs maps a NOT NULL TIMESTAMPTZ value back onto a proto timestamp.
func goValToTs(t time.Time) *timestamppb.Timestamp {
	if t.IsZero() {
		return nil
	}
	return timestamppb.New(t)
}

// dateToGoVal maps a proto date onto a NOT NULL DATE value (UTC midnight).
func dateToGoVal(d *date.Date) time.Time {
	if d == nil || (d.GetYear() == 0 && d.GetMonth() == 0 && d.GetDay() == 0) {
		return time.Time{}
	}
	return time.Date(int(d.GetYear()), time.Month(d.GetMonth()), int(d.GetDay()), 0, 0, 0, 0, time.UTC)
}

// goValToDate maps a NOT NULL DATE value back onto a proto date.
func goValToDate(t time.Time) *date.Date {
	if t.IsZero() {
		return nil
	}
	return &date.Date{Year: int32(t.Year()), Month: int32(t.Month()), Day: int32(t.Day())}
}

// enumsToCSV stores a repeated proto enum as the comma-joined list of its
// value names; nil for an empty list, so the column stays NULL.
func enumsToCSV[E interface {
	~int32
	fmt.Stringer
}](vs []E) *string {
	if len(vs) == 0 {
		return nil
	}
	parts := make([]string, 0, len(vs))
	for _, v := range vs {
		parts = append(parts, v.String())
	}
	s := strings.Join(parts, ",")
	return &s
}

// enumsFromCSV parses a stored comma-joined list of proto enum value names
// back onto the repeated enum, resolving each name through the enum's value
// map; unknown names map to 0.
func enumsFromCSV[E ~int32](s *string, byName map[string]int32) []E {
	if s == nil || *s == "" {
		return nil
	}
	names := strings.Split(*s, ",")
	out := make([]E, 0, len(names))
	for _, n := range names {
		out = append(out, E(byName[strings.TrimSpace(n)]))
	}
	return out
}
