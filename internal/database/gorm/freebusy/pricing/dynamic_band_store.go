// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: freebusy/pricing/v1/pricing.proto
//
// database: freebusy
// schema:   pricing
//
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package pricing

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// DynamicBandStoreIface is the full data-access surface of DynamicBand: everything
// DynamicBandStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedDynamicBandStore struct {
//		DynamicBandStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewDynamicBandStore returns the concrete
// *DynamicBandStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type DynamicBandStoreIface interface {
	Create(ctx context.Context, m *DynamicBand) error
	List(ctx context.Context, opts gormx.ListOptions) ([]DynamicBand, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *DynamicBand) error
	GetByID(ctx context.Context, id string) (*DynamicBand, error)
	DeleteByID(ctx context.Context, id string) error
	ListByDynamicRuleID(ctx context.Context, id string, opts gormx.ListOptions) ([]DynamicBand, error)
	ListByAdjustmentID(ctx context.Context, id string, opts gormx.ListOptions) ([]DynamicBand, error)
}

// DynamicBandStore provides typed CRUD access to DynamicBand records.
// One threshold band of a DynamicRule: the range the measured value must fall in, and what happens to the price when it does.
type DynamicBandStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewDynamicBandStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that DynamicBandStore implements its own interface. Without it a
// signature could drift from DynamicBandStoreIface and only break in whichever downstream
// package decorates it.
var _ DynamicBandStoreIface = (*DynamicBandStore)(nil)

// Compile-time proof that DynamicBandStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[DynamicBand] = (*DynamicBandStore)(nil)

// NewDynamicBandStore returns a DynamicBandStore backed by db.
func NewDynamicBandStore(db *gorm.DB) *DynamicBandStore { return &DynamicBandStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *DynamicBandStore) WithTelemetry(t gormx.Telemetry) *DynamicBandStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *DynamicBandStore) Create(ctx context.Context, m *DynamicBand) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicBand/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_bands", "create", time.Since(start), err)
	return err
}

// List returns the DynamicBand records matching opts.
func (s *DynamicBandStore) List(ctx context.Context, opts gormx.ListOptions) ([]DynamicBand, error) {
	var out []DynamicBand
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicBand/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_bands", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of DynamicBand records matching opts.Where
// (pagination and ordering are ignored).
func (s *DynamicBandStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicBand/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&DynamicBand{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_bands", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *DynamicBandStore) Update(ctx context.Context, m *DynamicBand) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicBand/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_bands", "update", time.Since(start), err)
	return err
}

// GetByID fetches the DynamicBand with the given primary key.
func (s *DynamicBandStore) GetByID(ctx context.Context, id string) (*DynamicBand, error) {
	var m DynamicBand
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicBand/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_bands", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the DynamicBand with the given primary key.
func (s *DynamicBandStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicBand/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&DynamicBand{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_bands", "delete", time.Since(start), err)
	return err
}

// ListByDynamicRuleID returns the DynamicBand records whose dynamic_rule_id matches id, with opts applied.
func (s *DynamicBandStore) ListByDynamicRuleID(ctx context.Context, id string, opts gormx.ListOptions) ([]DynamicBand, error) {
	var out []DynamicBand
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicBand/ListByDynamicRuleID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("dynamic_rule_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_bands", "list_by_dynamic_rule_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ListByAdjustmentID returns the DynamicBand records whose adjustment_id matches id, with opts applied.
func (s *DynamicBandStore) ListByAdjustmentID(ctx context.Context, id string, opts gormx.ListOptions) ([]DynamicBand, error) {
	var out []DynamicBand
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicBand/ListByAdjustmentID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("adjustment_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_bands", "list_by_adjustment_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}
