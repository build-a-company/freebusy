// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: freebusy/pricing/v1/pricing.proto
//
// database: freebusy
// schema:   pricing
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package pricing

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// DynamicRuleStoreIface is the full data-access surface of DynamicRule: everything
// DynamicRuleStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedDynamicRuleStore struct {
//		DynamicRuleStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewDynamicRuleStore returns the concrete
// *DynamicRuleStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type DynamicRuleStoreIface interface {
	Create(ctx context.Context, m *DynamicRule) error
	List(ctx context.Context, opts gormx.ListOptions) ([]DynamicRule, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *DynamicRule) error
	GetByID(ctx context.Context, id string) (*DynamicRule, error)
	DeleteByID(ctx context.Context, id string) error
	ListByRatePlanID(ctx context.Context, id string, opts gormx.ListOptions) ([]DynamicRule, error)
}

// DynamicRuleStore provides typed CRUD access to DynamicRule records.
// A rule that moves price according to something measured at quote time. Bands are evaluated against the trigger's measured value and at most one band matches per rule, because bands of a single rule partition a range. Rules compound across triggers; bands do not compound within one.
type DynamicRuleStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewDynamicRuleStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that DynamicRuleStore implements its own interface. Without it a
// signature could drift from DynamicRuleStoreIface and only break in whichever downstream
// package decorates it.
var _ DynamicRuleStoreIface = (*DynamicRuleStore)(nil)

// Compile-time proof that DynamicRuleStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[DynamicRule] = (*DynamicRuleStore)(nil)

// NewDynamicRuleStore returns a DynamicRuleStore backed by db.
func NewDynamicRuleStore(db *gorm.DB) *DynamicRuleStore { return &DynamicRuleStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *DynamicRuleStore) WithTelemetry(t gormx.Telemetry) *DynamicRuleStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *DynamicRuleStore) Create(ctx context.Context, m *DynamicRule) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicRule/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_rules", "create", time.Since(start), err)
	return err
}

// List returns the DynamicRule records matching opts.
func (s *DynamicRuleStore) List(ctx context.Context, opts gormx.ListOptions) ([]DynamicRule, error) {
	var out []DynamicRule
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicRule/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_rules", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of DynamicRule records matching opts.Where
// (pagination and ordering are ignored).
func (s *DynamicRuleStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicRule/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&DynamicRule{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_rules", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *DynamicRuleStore) Update(ctx context.Context, m *DynamicRule) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicRule/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_rules", "update", time.Since(start), err)
	return err
}

// GetByID fetches the DynamicRule with the given primary key.
func (s *DynamicRuleStore) GetByID(ctx context.Context, id string) (*DynamicRule, error) {
	var m DynamicRule
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicRule/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_rules", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the DynamicRule with the given primary key.
func (s *DynamicRuleStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicRule/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&DynamicRule{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_rules", "delete", time.Since(start), err)
	return err
}

// ListByRatePlanID returns the DynamicRule records whose rate_plan_id matches id, with opts applied.
func (s *DynamicRuleStore) ListByRatePlanID(ctx context.Context, id string, opts gormx.ListOptions) ([]DynamicRule, error) {
	var out []DynamicRule
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "pricing.DynamicRule/ListByRatePlanID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("rate_plan_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "pricing.dynamic_rules", "list_by_rate_plan_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}
