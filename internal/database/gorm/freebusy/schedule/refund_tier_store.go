// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.1
// 	protoc (unknown)
// source: freebusy/schedule/v1/schedule.proto
//
// database: freebusy
// schema:   schedule
//
// annotations: entity.v1 v1.5.1, store.v1 v1.5.1
// engine:      protokit v1.2.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package schedule

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// RefundTierStoreIface is the full data-access surface of RefundTier: everything
// RefundTierStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedRefundTierStore struct {
//		RefundTierStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewRefundTierStore returns the concrete
// *RefundTierStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type RefundTierStoreIface interface {
	Create(ctx context.Context, m *RefundTier) error
	List(ctx context.Context, opts gormx.ListOptions) ([]RefundTier, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *RefundTier) error
	GetByID(ctx context.Context, id string) (*RefundTier, error)
	DeleteByID(ctx context.Context, id string) error
	ListByCancellationPolicyID(ctx context.Context, id string, opts gormx.ListOptions) ([]RefundTier, error)
}

// RefundTierStore provides typed CRUD access to RefundTier records.
// One tier of a CancellationPolicy: cancel at least `cutoff` before the booking start to receive `refund_percent` of the total back.
type RefundTierStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewRefundTierStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that RefundTierStore implements its own interface. Without it a
// signature could drift from RefundTierStoreIface and only break in whichever downstream
// package decorates it.
var _ RefundTierStoreIface = (*RefundTierStore)(nil)

// Compile-time proof that RefundTierStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[RefundTier] = (*RefundTierStore)(nil)

// NewRefundTierStore returns a RefundTierStore backed by db.
func NewRefundTierStore(db *gorm.DB) *RefundTierStore { return &RefundTierStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *RefundTierStore) WithTelemetry(t gormx.Telemetry) *RefundTierStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *RefundTierStore) Create(ctx context.Context, m *RefundTier) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.RefundTier/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "schedule.refund_tiers", "create", time.Since(start), err)
	return err
}

// List returns the RefundTier records matching opts.
func (s *RefundTierStore) List(ctx context.Context, opts gormx.ListOptions) ([]RefundTier, error) {
	var out []RefundTier
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.RefundTier/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "schedule.refund_tiers", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of RefundTier records matching opts.Where
// (pagination and ordering are ignored).
func (s *RefundTierStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.RefundTier/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&RefundTier{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "schedule.refund_tiers", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *RefundTierStore) Update(ctx context.Context, m *RefundTier) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.RefundTier/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "schedule.refund_tiers", "update", time.Since(start), err)
	return err
}

// GetByID fetches the RefundTier with the given primary key.
func (s *RefundTierStore) GetByID(ctx context.Context, id string) (*RefundTier, error) {
	var m RefundTier
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.RefundTier/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "schedule.refund_tiers", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the RefundTier with the given primary key.
func (s *RefundTierStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.RefundTier/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&RefundTier{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "schedule.refund_tiers", "delete", time.Since(start), err)
	return err
}

// ListByCancellationPolicyID returns the RefundTier records whose cancellation_policy_id matches id, with opts applied.
func (s *RefundTierStore) ListByCancellationPolicyID(ctx context.Context, id string, opts gormx.ListOptions) ([]RefundTier, error) {
	var out []RefundTier
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.RefundTier/ListByCancellationPolicyID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("cancellation_policy_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "schedule.refund_tiers", "list_by_cancellation_policy_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}
