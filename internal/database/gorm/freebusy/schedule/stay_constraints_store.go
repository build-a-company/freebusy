// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: freebusy/schedule/v1/schedule.proto
//
// database: freebusy
// schema:   schedule
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package schedule

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// StayConstraintsStoreIface is the full data-access surface of StayConstraints: everything
// StayConstraintsStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedStayConstraintsStore struct {
//		StayConstraintsStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewStayConstraintsStore returns the concrete
// *StayConstraintsStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type StayConstraintsStoreIface interface {
	Create(ctx context.Context, m *StayConstraints) error
	List(ctx context.Context, opts gormx.ListOptions) ([]StayConstraints, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *StayConstraints) error
	GetByID(ctx context.Context, id string) (*StayConstraints, error)
	DeleteByID(ctx context.Context, id string) error
}

// StayConstraintsStore provides typed CRUD access to StayConstraints records.
// Stay rules that affect bookability for NIGHTLY units.
type StayConstraintsStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewStayConstraintsStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that StayConstraintsStore implements its own interface. Without it a
// signature could drift from StayConstraintsStoreIface and only break in whichever downstream
// package decorates it.
var _ StayConstraintsStoreIface = (*StayConstraintsStore)(nil)

// Compile-time proof that StayConstraintsStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[StayConstraints] = (*StayConstraintsStore)(nil)

// NewStayConstraintsStore returns a StayConstraintsStore backed by db.
func NewStayConstraintsStore(db *gorm.DB) *StayConstraintsStore { return &StayConstraintsStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *StayConstraintsStore) WithTelemetry(t gormx.Telemetry) *StayConstraintsStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *StayConstraintsStore) Create(ctx context.Context, m *StayConstraints) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.StayConstraints/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "schedule.stay_constraints", "create", time.Since(start), err)
	return err
}

// List returns the StayConstraints records matching opts.
func (s *StayConstraintsStore) List(ctx context.Context, opts gormx.ListOptions) ([]StayConstraints, error) {
	var out []StayConstraints
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.StayConstraints/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "schedule.stay_constraints", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of StayConstraints records matching opts.Where
// (pagination and ordering are ignored).
func (s *StayConstraintsStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.StayConstraints/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&StayConstraints{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "schedule.stay_constraints", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *StayConstraintsStore) Update(ctx context.Context, m *StayConstraints) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.StayConstraints/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "schedule.stay_constraints", "update", time.Since(start), err)
	return err
}

// GetByID fetches the StayConstraints with the given primary key.
func (s *StayConstraintsStore) GetByID(ctx context.Context, id string) (*StayConstraints, error) {
	var m StayConstraints
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.StayConstraints/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "schedule.stay_constraints", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the StayConstraints with the given primary key.
func (s *StayConstraintsStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "schedule.StayConstraints/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&StayConstraints{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "schedule.stay_constraints", "delete", time.Since(start), err)
	return err
}
