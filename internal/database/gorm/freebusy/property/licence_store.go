// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.1
// 	protoc (unknown)
// source: freebusy/property/v1/property.proto
//
// database: freebusy
// schema:   property
//
// annotations: entity.v1 v1.5.1, store.v1 v1.5.1
// engine:      protokit v1.2.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package property

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// LicenceStoreIface is the full data-access surface of Licence: everything
// LicenceStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedLicenceStore struct {
//		LicenceStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewLicenceStore returns the concrete
// *LicenceStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type LicenceStoreIface interface {
	Create(ctx context.Context, m *Licence) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Licence, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Licence) error
	GetByID(ctx context.Context, id string) (*Licence, error)
	DeleteByID(ctx context.Context, id string) error
	GetByName(ctx context.Context, v string) (*Licence, error)
	ListByUnitID(ctx context.Context, id string, opts gormx.ListOptions) ([]Licence, error)
	ListByPropertyID(ctx context.Context, id string, opts gormx.ListOptions) ([]Licence, error)
	ListByAttachmentID(ctx context.Context, id string, opts gormx.ListOptions) ([]Licence, error)
}

// LicenceStore provides typed CRUD access to Licence records.
// A regulatory licence or certificate held by a Property or one of its Units (e.g. trade licence, fire safety NOC, per-room liquor licence). One resource covers both: every licence is parented by the property, `target` says what it covers, and a unit licence names its unit in `unit`. Tracks the issuing authority and validity window so `expiry_date` can be filtered on to find licences due for renewal; the certificate itself is carried in `attachment`. A standalone resource (own Get/List/Create/Update/Delete), like Unit, rather than an embedded repeated field like Media, so a renewal doesn't require resending the whole Property.
type LicenceStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewLicenceStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that LicenceStore implements its own interface. Without it a
// signature could drift from LicenceStoreIface and only break in whichever downstream
// package decorates it.
var _ LicenceStoreIface = (*LicenceStore)(nil)

// Compile-time proof that LicenceStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Licence] = (*LicenceStore)(nil)

// NewLicenceStore returns a LicenceStore backed by db.
func NewLicenceStore(db *gorm.DB) *LicenceStore { return &LicenceStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *LicenceStore) WithTelemetry(t gormx.Telemetry) *LicenceStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *LicenceStore) Create(ctx context.Context, m *Licence) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "property.licences", "create", time.Since(start), err)
	return err
}

// List returns the Licence records matching opts.
func (s *LicenceStore) List(ctx context.Context, opts gormx.ListOptions) ([]Licence, error) {
	var out []Licence
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "property.licences", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Licence records matching opts.Where
// (pagination and ordering are ignored).
func (s *LicenceStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&Licence{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "property.licences", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *LicenceStore) Update(ctx context.Context, m *Licence) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "property.licences", "update", time.Since(start), err)
	return err
}

// GetByID fetches the Licence with the given primary key.
func (s *LicenceStore) GetByID(ctx context.Context, id string) (*Licence, error) {
	var m Licence
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "property.licences", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Licence with the given primary key.
func (s *LicenceStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&Licence{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "property.licences", "delete", time.Since(start), err)
	return err
}

// GetByName fetches the Licence with the given name (a unique column).
func (s *LicenceStore) GetByName(ctx context.Context, v string) (*Licence, error) {
	var m Licence
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/GetByName", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "name = ?", v).Error
	})
	tel.RecordOp(ctx, "property.licences", "get_by_name", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// ListByUnitID returns the Licence records whose unit matches id, with opts applied.
func (s *LicenceStore) ListByUnitID(ctx context.Context, id string, opts gormx.ListOptions) ([]Licence, error) {
	var out []Licence
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/ListByUnitID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("unit = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "property.licences", "list_by_unit", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ListByPropertyID returns the Licence records whose property_id matches id, with opts applied.
func (s *LicenceStore) ListByPropertyID(ctx context.Context, id string, opts gormx.ListOptions) ([]Licence, error) {
	var out []Licence
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/ListByPropertyID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("property_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "property.licences", "list_by_property_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ListByAttachmentID returns the Licence records whose attachment_id matches id, with opts applied.
func (s *LicenceStore) ListByAttachmentID(ctx context.Context, id string, opts gormx.ListOptions) ([]Licence, error) {
	var out []Licence
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.Licence/ListByAttachmentID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("attachment_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "property.licences", "list_by_attachment_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}
