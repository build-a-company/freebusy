// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.1
// 	protoc (unknown)
// source: freebusy/property/v1/property.proto
//
// database: freebusy
// schema:   property
//
// annotations: entity.v1 v1.5.1, store.v1 v1.5.1
// engine:      protokit v1.2.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package property

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// UnitMediaStoreIface is the full data-access surface of UnitMedia: everything
// UnitMediaStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedUnitMediaStore struct {
//		UnitMediaStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewUnitMediaStore returns the concrete
// *UnitMediaStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type UnitMediaStoreIface interface {
	Create(ctx context.Context, m *UnitMedia) error
	List(ctx context.Context, opts gormx.ListOptions) ([]UnitMedia, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *UnitMedia) error
	GetByID(ctx context.Context, id string) (*UnitMedia, error)
	DeleteByID(ctx context.Context, id string) error
	ListByUnitID(ctx context.Context, id string, opts gormx.ListOptions) ([]UnitMedia, error)
}

// UnitMediaStore provides typed CRUD access to UnitMedia records.
// A media asset in a Unit's gallery. Identical in shape to `Media`; kept a separate message so the ORM gives it its own child table owned solely by the Unit (a single unit_id foreign key).
type UnitMediaStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewUnitMediaStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that UnitMediaStore implements its own interface. Without it a
// signature could drift from UnitMediaStoreIface and only break in whichever downstream
// package decorates it.
var _ UnitMediaStoreIface = (*UnitMediaStore)(nil)

// Compile-time proof that UnitMediaStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[UnitMedia] = (*UnitMediaStore)(nil)

// NewUnitMediaStore returns a UnitMediaStore backed by db.
func NewUnitMediaStore(db *gorm.DB) *UnitMediaStore { return &UnitMediaStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *UnitMediaStore) WithTelemetry(t gormx.Telemetry) *UnitMediaStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *UnitMediaStore) Create(ctx context.Context, m *UnitMedia) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.UnitMedia/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "property.unit_medias", "create", time.Since(start), err)
	return err
}

// List returns the UnitMedia records matching opts.
func (s *UnitMediaStore) List(ctx context.Context, opts gormx.ListOptions) ([]UnitMedia, error) {
	var out []UnitMedia
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.UnitMedia/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "property.unit_medias", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of UnitMedia records matching opts.Where
// (pagination and ordering are ignored).
func (s *UnitMediaStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.UnitMedia/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&UnitMedia{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "property.unit_medias", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *UnitMediaStore) Update(ctx context.Context, m *UnitMedia) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.UnitMedia/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "property.unit_medias", "update", time.Since(start), err)
	return err
}

// GetByID fetches the UnitMedia with the given primary key.
func (s *UnitMediaStore) GetByID(ctx context.Context, id string) (*UnitMedia, error) {
	var m UnitMedia
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.UnitMedia/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "property.unit_medias", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the UnitMedia with the given primary key.
func (s *UnitMediaStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.UnitMedia/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&UnitMedia{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "property.unit_medias", "delete", time.Since(start), err)
	return err
}

// ListByUnitID returns the UnitMedia records whose unit_id matches id, with opts applied.
func (s *UnitMediaStore) ListByUnitID(ctx context.Context, id string, opts gormx.ListOptions) ([]UnitMedia, error) {
	var out []UnitMedia
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "property.UnitMedia/ListByUnitID", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx).Where("unit_id = ?", id)).Find(&out).Error
	})
	tel.RecordOp(ctx, "property.unit_medias", "list_by_unit_id", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}
