// Code generated by protoc-gen-orm. DO NOT EDIT.
// versions:
// 	protoc-gen-orm dev
// 	protoc (unknown)
// source: freebusy/organisation/v1/organisation.proto
//
// database: freebusy
// schema:   organisation
//
// orm — https://github.com/the-protobuf-project/orm

package organisation

import (
	"context"
	"time"

	"github.com/oh-tarnished/freebusy/internal/database/gorm/gormx"
	"gorm.io/gorm"
)

// OrganisationStore provides typed CRUD access to Organisation records.
// A chain: the hotel brand/company that owns one or more properties, and the unit of multi-tenancy. The shell enforces isolation with row-level security keyed off the caller's organisation. Each Property references the Organisation it belongs to; members here are the chain's administrators, not its guests (guests relate to a property only through bookings).
type OrganisationStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewOrganisationStore(db).WithTelemetry(ormtelemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that OrganisationStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Organisation] = (*OrganisationStore)(nil)

// NewOrganisationStore returns a OrganisationStore backed by db.
func NewOrganisationStore(db *gorm.DB) *OrganisationStore { return &OrganisationStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *OrganisationStore) WithTelemetry(t gormx.Telemetry) *OrganisationStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *OrganisationStore) Create(ctx context.Context, m *Organisation) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "organisation.Organisation/Create", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Create(m).Error
	})
	tel.RecordOp(ctx, "organisation.resource", "create", time.Since(start), err)
	return err
}

// List returns the Organisation records matching opts.
func (s *OrganisationStore) List(ctx context.Context, opts gormx.ListOptions) ([]Organisation, error) {
	var out []Organisation
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "organisation.Organisation/List", nil, func(ctx context.Context) error {
		return opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error
	})
	tel.RecordOp(ctx, "organisation.resource", "list", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Organisation records matching opts.Where
// (pagination and ordering are ignored).
func (s *OrganisationStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "organisation.Organisation/Count", nil, func(ctx context.Context) error {
		db := s.DB.WithContext(ctx).Model(&Organisation{})
		if opts.Where != nil {
			db = db.Where(opts.Where, opts.Args...)
		}
		return db.Count(&n).Error
	})
	tel.RecordOp(ctx, "organisation.resource", "count", time.Since(start), err)
	if err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *OrganisationStore) Update(ctx context.Context, m *Organisation) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "organisation.Organisation/Update", m, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Save(m).Error
	})
	tel.RecordOp(ctx, "organisation.resource", "update", time.Since(start), err)
	return err
}

// GetByID fetches the Organisation with the given primary key.
func (s *OrganisationStore) GetByID(ctx context.Context, id string) (*Organisation, error) {
	var m Organisation
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "organisation.Organisation/GetByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "organisation.resource", "get", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Organisation with the given primary key.
func (s *OrganisationStore) DeleteByID(ctx context.Context, id string) error {
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "organisation.Organisation/DeleteByID", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).Delete(&Organisation{}, "id = ?", id).Error
	})
	tel.RecordOp(ctx, "organisation.resource", "delete", time.Since(start), err)
	return err
}

// GetByName fetches the Organisation with the given name (a unique column).
func (s *OrganisationStore) GetByName(ctx context.Context, v string) (*Organisation, error) {
	var m Organisation
	tel := gormx.OrNop(s.Telemetry)
	start := time.Now()
	err := tel.Span(ctx, "organisation.Organisation/GetByName", nil, func(ctx context.Context) error {
		return s.DB.WithContext(ctx).First(&m, "name = ?", v).Error
	})
	tel.RecordOp(ctx, "organisation.resource", "get_by_name", time.Since(start), err)
	if err != nil {
		return nil, err
	}
	return &m, nil
}
