// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: protobuf/rfc7953/availability/v1/availability.proto, protobuf/rfc7953/availability/v1/available.proto, protobuf/rfc7953/availability/v1/types.proto
//
// database: rfc
// schema:   availability
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package availability

import (
	"context"

	"github.com/oh-tarnished/freebusy/internal/database/rfcgorm/gormx"
	"gorm.io/gorm"
)

// AvailablePeriodStoreIface is the full data-access surface of AvailablePeriod: everything
// AvailablePeriodStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedAvailablePeriodStore struct {
//		AvailablePeriodStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewAvailablePeriodStore returns the concrete
// *AvailablePeriodStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type AvailablePeriodStoreIface interface {
	Create(ctx context.Context, m *AvailablePeriod) error
	List(ctx context.Context, opts gormx.ListOptions) ([]AvailablePeriod, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *AvailablePeriod) error
	GetByID(ctx context.Context, id string) (*AvailablePeriod, error)
	DeleteByID(ctx context.Context, id string) error
	ListByAvailabilityID(ctx context.Context, id string, opts gormx.ListOptions) ([]AvailablePeriod, error)
	ListByStartID(ctx context.Context, id string, opts gormx.ListOptions) ([]AvailablePeriod, error)
	ListByEndID(ctx context.Context, id string, opts gormx.ListOptions) ([]AvailablePeriod, error)
}

// AvailablePeriodStore provides typed CRUD access to AvailablePeriod records.
// AvailablePeriod is the AVAILABLE sub-component, RFC 7953 section 3.1 <https://www.rfc-editor.org/rfc/rfc7953.html#section-3.1>. A window of free time inside an Availability. The enclosing VAVAILABILITY declares its whole range busy by default, and each of these carves out a period that is not -- so an ordinary working week is one Availability with one recurring AvailablePeriod, not five hundred separate ones. A value object, not a resource: section 3.1 defines it only as a sub-component of VAVAILABILITY, and it has no identity or lifecycle apart from its parent. Its own UID (`ical_uid`) is interchange identity, the same distinction rule 9 draws everywhere else. Named AvailablePeriod rather than Available: AIP-140 <https://aip.dev/140> wants a noun, and a bare adjective names no thing. Reference: RFC 7953 "Calendar Availability", section 3.1. https://www.rfc-editor.org/rfc/rfc7953.html#section-3.1
type AvailablePeriodStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewAvailablePeriodStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that AvailablePeriodStore implements its own interface. Without it a
// signature could drift from AvailablePeriodStoreIface and only break in whichever downstream
// package decorates it.
var _ AvailablePeriodStoreIface = (*AvailablePeriodStore)(nil)

// Compile-time proof that AvailablePeriodStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[AvailablePeriod] = (*AvailablePeriodStore)(nil)

// NewAvailablePeriodStore returns a AvailablePeriodStore backed by db.
func NewAvailablePeriodStore(db *gorm.DB) *AvailablePeriodStore { return &AvailablePeriodStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *AvailablePeriodStore) WithTelemetry(t gormx.Telemetry) *AvailablePeriodStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *AvailablePeriodStore) Create(ctx context.Context, m *AvailablePeriod) error {
	return s.DB.WithContext(ctx).Create(m).Error
}

// List returns the AvailablePeriod records matching opts.
func (s *AvailablePeriodStore) List(ctx context.Context, opts gormx.ListOptions) ([]AvailablePeriod, error) {
	var out []AvailablePeriod
	if err := opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of AvailablePeriod records matching opts.Where
// (pagination and ordering are ignored).
func (s *AvailablePeriodStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	db := s.DB.WithContext(ctx).Model(&AvailablePeriod{})
	if opts.Where != nil {
		db = db.Where(opts.Where, opts.Args...)
	}
	if err := db.Count(&n).Error; err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *AvailablePeriodStore) Update(ctx context.Context, m *AvailablePeriod) error {
	return s.DB.WithContext(ctx).Save(m).Error
}

// GetByID fetches the AvailablePeriod with the given primary key.
func (s *AvailablePeriodStore) GetByID(ctx context.Context, id string) (*AvailablePeriod, error) {
	var m AvailablePeriod
	if err := s.DB.WithContext(ctx).First(&m, "id = ?", id).Error; err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the AvailablePeriod with the given primary key.
func (s *AvailablePeriodStore) DeleteByID(ctx context.Context, id string) error {
	return s.DB.WithContext(ctx).Delete(&AvailablePeriod{}, "id = ?", id).Error
}

// ListByAvailabilityID returns the AvailablePeriod records whose availability_id matches id, with opts applied.
func (s *AvailablePeriodStore) ListByAvailabilityID(ctx context.Context, id string, opts gormx.ListOptions) ([]AvailablePeriod, error) {
	var out []AvailablePeriod
	q := opts.Apply(s.DB.WithContext(ctx).Where("availability_id = ?", id))
	if err := q.Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}

// ListByStartID returns the AvailablePeriod records whose start_id matches id, with opts applied.
func (s *AvailablePeriodStore) ListByStartID(ctx context.Context, id string, opts gormx.ListOptions) ([]AvailablePeriod, error) {
	var out []AvailablePeriod
	q := opts.Apply(s.DB.WithContext(ctx).Where("start_id = ?", id))
	if err := q.Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}

// ListByEndID returns the AvailablePeriod records whose end_id matches id, with opts applied.
func (s *AvailablePeriodStore) ListByEndID(ctx context.Context, id string, opts gormx.ListOptions) ([]AvailablePeriod, error) {
	var out []AvailablePeriod
	q := opts.Apply(s.DB.WithContext(ctx).Where("end_id = ?", id))
	if err := q.Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}
