// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: protobuf/rfc6350/vcard/v1/contact.proto, protobuf/rfc6350/vcard/v1/contact_address.proto, protobuf/rfc6350/vcard/v1/contact_location.proto, protobuf/rfc6350/vcard/v1/contact_method.proto, protobuf/rfc6350/vcard/v1/contact_name.proto, protobuf/rfc6350/vcard/v1/contact_property.proto, protobuf/rfc6350/vcard/v1/contact_relation.proto, protobuf/rfc6350/vcard/v1/contact_speech.proto, protobuf/rfc6350/vcard/v1/date_or_text.proto, protobuf/rfc6350/vcard/v1/types.proto, protobuf/rfc6350/vcard/v1/vcard.proto
//
// database: rfc
// schema:   vcard
//
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package vcard

import (
	"context"

	"github.com/oh-tarnished/freebusy/internal/database/rfcgorm/gormx"
	"gorm.io/gorm"
)

// DateOrTextStoreIface is the full data-access surface of DateOrText: everything
// DateOrTextStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedDateOrTextStore struct {
//		DateOrTextStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewDateOrTextStore returns the concrete
// *DateOrTextStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type DateOrTextStoreIface interface {
	Create(ctx context.Context, m *DateOrText) error
	List(ctx context.Context, opts gormx.ListOptions) ([]DateOrText, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *DateOrText) error
	GetByID(ctx context.Context, id string) (*DateOrText, error)
	DeleteByID(ctx context.Context, id string) error
}

// DateOrTextStore provides typed CRUD access to DateOrText records.
// DateOrText is vCard's DATE-AND-OR-TIME value type, RFC 6350 section 4.3.4 <https://www.rfc-editor.org/rfc/rfc6350.html#section-4.3.4>, including the text alternative every property using it permits. Three branches because the RFC needs all three: date       "19960415", and crucially "--0415" -- a birthday with no year, which is the common case in an address book. google.type.Date allows year 0 for exactly this; a Timestamp cannot express it. date_time  "19531015T231000Z", when a time of day is recorded too. text       "circa 1800". Section 6.2.5 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.2.5> explicitly permits resetting the value to text, and a schema that drops it loses data a conforming vCard is allowed to carry. Reference: RFC 6350 "vCard Format Specification", section 4.3.4. https://www.rfc-editor.org/rfc/rfc6350.html#section-4.3.4
type DateOrTextStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewDateOrTextStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that DateOrTextStore implements its own interface. Without it a
// signature could drift from DateOrTextStoreIface and only break in whichever downstream
// package decorates it.
var _ DateOrTextStoreIface = (*DateOrTextStore)(nil)

// Compile-time proof that DateOrTextStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[DateOrText] = (*DateOrTextStore)(nil)

// NewDateOrTextStore returns a DateOrTextStore backed by db.
func NewDateOrTextStore(db *gorm.DB) *DateOrTextStore { return &DateOrTextStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *DateOrTextStore) WithTelemetry(t gormx.Telemetry) *DateOrTextStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *DateOrTextStore) Create(ctx context.Context, m *DateOrText) error {
	return s.DB.WithContext(ctx).Create(m).Error
}

// List returns the DateOrText records matching opts.
func (s *DateOrTextStore) List(ctx context.Context, opts gormx.ListOptions) ([]DateOrText, error) {
	var out []DateOrText
	if err := opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of DateOrText records matching opts.Where
// (pagination and ordering are ignored).
func (s *DateOrTextStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	db := s.DB.WithContext(ctx).Model(&DateOrText{})
	if opts.Where != nil {
		db = db.Where(opts.Where, opts.Args...)
	}
	if err := db.Count(&n).Error; err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *DateOrTextStore) Update(ctx context.Context, m *DateOrText) error {
	return s.DB.WithContext(ctx).Save(m).Error
}

// GetByID fetches the DateOrText with the given primary key.
func (s *DateOrTextStore) GetByID(ctx context.Context, id string) (*DateOrText, error) {
	var m DateOrText
	if err := s.DB.WithContext(ctx).First(&m, "id = ?", id).Error; err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the DateOrText with the given primary key.
func (s *DateOrTextStore) DeleteByID(ctx context.Context, id string) error {
	return s.DB.WithContext(ctx).Delete(&DateOrText{}, "id = ?", id).Error
}
