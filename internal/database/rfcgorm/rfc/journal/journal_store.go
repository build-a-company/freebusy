// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: protobuf/rfc5545/journal/v1/journal.proto, protobuf/rfc5545/journal/v1/types.proto
//
// database: rfc
// schema:   journal
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package journal

import (
	"context"

	"github.com/oh-tarnished/freebusy/internal/database/rfcgorm/gormx"
	"gorm.io/gorm"
)

// JournalStoreIface is the full data-access surface of Journal: everything
// JournalStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedJournalStore struct {
//		JournalStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewJournalStore returns the concrete
// *JournalStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type JournalStoreIface interface {
	Create(ctx context.Context, m *Journal) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Journal, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Journal) error
	GetByID(ctx context.Context, id string) (*Journal, error)
	DeleteByID(ctx context.Context, id string) error
	GetByName(ctx context.Context, v string) (*Journal, error)
	GetByUID(ctx context.Context, v string) (*Journal, error)
	ListByCalendarID(ctx context.Context, id string, opts gormx.ListOptions) ([]Journal, error)
	ListByStartID(ctx context.Context, id string, opts gormx.ListOptions) ([]Journal, error)
}

// JournalStore provides typed CRUD access to Journal records.
// Journal is the VJOURNAL component, RFC 5545 section 3.6.3 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.6.3>. A dated note attached to a calendar. Unlike Event and Todo it consumes no time -- section 3.6.3 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.6.3> gives it no DTEND, DURATION or DUE, and it never affects free/busy. Scope note: its Classification enum is a copy of the one in protobuf.rfc5545.event.v1, not a reference. AIP-215 permits no cross-package message reference, so value types shared between iCalendar components are duplicated per component package. Reference: RFC 5545 "Internet Calendaring and Scheduling Core Object Specification (iCalendar)", section 3.6.3. https://www.rfc-editor.org/rfc/rfc5545.html#section-3.6.3
type JournalStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewJournalStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that JournalStore implements its own interface. Without it a
// signature could drift from JournalStoreIface and only break in whichever downstream
// package decorates it.
var _ JournalStoreIface = (*JournalStore)(nil)

// Compile-time proof that JournalStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Journal] = (*JournalStore)(nil)

// NewJournalStore returns a JournalStore backed by db.
func NewJournalStore(db *gorm.DB) *JournalStore { return &JournalStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *JournalStore) WithTelemetry(t gormx.Telemetry) *JournalStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *JournalStore) Create(ctx context.Context, m *Journal) error {
	return s.DB.WithContext(ctx).Create(m).Error
}

// List returns the Journal records matching opts.
func (s *JournalStore) List(ctx context.Context, opts gormx.ListOptions) ([]Journal, error) {
	var out []Journal
	if err := opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Journal records matching opts.Where
// (pagination and ordering are ignored).
func (s *JournalStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	db := s.DB.WithContext(ctx).Model(&Journal{})
	if opts.Where != nil {
		db = db.Where(opts.Where, opts.Args...)
	}
	if err := db.Count(&n).Error; err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *JournalStore) Update(ctx context.Context, m *Journal) error {
	return s.DB.WithContext(ctx).Save(m).Error
}

// GetByID fetches the Journal with the given primary key.
func (s *JournalStore) GetByID(ctx context.Context, id string) (*Journal, error) {
	var m Journal
	if err := s.DB.WithContext(ctx).First(&m, "id = ?", id).Error; err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Journal with the given primary key.
func (s *JournalStore) DeleteByID(ctx context.Context, id string) error {
	return s.DB.WithContext(ctx).Delete(&Journal{}, "id = ?", id).Error
}

// GetByName fetches the Journal with the given name (a unique column).
func (s *JournalStore) GetByName(ctx context.Context, v string) (*Journal, error) {
	var m Journal
	if err := s.DB.WithContext(ctx).First(&m, "name = ?", v).Error; err != nil {
		return nil, err
	}
	return &m, nil
}

// GetByUID fetches the Journal with the given uid (a unique column).
func (s *JournalStore) GetByUID(ctx context.Context, v string) (*Journal, error) {
	var m Journal
	if err := s.DB.WithContext(ctx).First(&m, "uid = ?", v).Error; err != nil {
		return nil, err
	}
	return &m, nil
}

// ListByCalendarID returns the Journal records whose calendar_id matches id, with opts applied.
func (s *JournalStore) ListByCalendarID(ctx context.Context, id string, opts gormx.ListOptions) ([]Journal, error) {
	var out []Journal
	q := opts.Apply(s.DB.WithContext(ctx).Where("calendar_id = ?", id))
	if err := q.Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}

// ListByStartID returns the Journal records whose start_id matches id, with opts applied.
func (s *JournalStore) ListByStartID(ctx context.Context, id string, opts gormx.ListOptions) ([]Journal, error) {
	var out []Journal
	q := opts.Apply(s.DB.WithContext(ctx).Where("start_id = ?", id))
	if err := q.Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}
