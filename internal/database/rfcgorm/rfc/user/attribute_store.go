// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: protobuf/rfc7643/user/v1/enterprise.proto, protobuf/rfc7643/user/v1/types.proto, protobuf/rfc7643/user/v1/user.proto
//
// database: rfc
// schema:   user
//
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package user

import (
	"context"

	"github.com/oh-tarnished/freebusy/internal/database/rfcgorm/gormx"
	"gorm.io/gorm"
)

// AttributeStoreIface is the full data-access surface of Attribute: everything
// AttributeStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedAttributeStore struct {
//		AttributeStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewAttributeStore returns the concrete
// *AttributeStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type AttributeStoreIface interface {
	Create(ctx context.Context, m *Attribute) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Attribute, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Attribute) error
	GetByID(ctx context.Context, id string) (*Attribute, error)
	DeleteByID(ctx context.Context, id string) error
	ListByUserID(ctx context.Context, id string, opts gormx.ListOptions) ([]Attribute, error)
}

// AttributeStore provides typed CRUD access to Attribute records.
// Attribute is SCIM's default multi-valued attribute, RFC 7643 section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. One shape for emails, phone numbers, IMs, photos, entitlements, roles and certificates alike -- section 2.4 defines `value`, `display`, `type` and `primary` once and every multi-valued attribute reuses them. That uniformity is deliberate in SCIM and is why this is a single message rather than one per attribute, which is the opposite of the choice RFC 9553 forced in the JSContact package. Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 2.4. https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4
type AttributeStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewAttributeStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that AttributeStore implements its own interface. Without it a
// signature could drift from AttributeStoreIface and only break in whichever downstream
// package decorates it.
var _ AttributeStoreIface = (*AttributeStore)(nil)

// Compile-time proof that AttributeStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Attribute] = (*AttributeStore)(nil)

// NewAttributeStore returns a AttributeStore backed by db.
func NewAttributeStore(db *gorm.DB) *AttributeStore { return &AttributeStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *AttributeStore) WithTelemetry(t gormx.Telemetry) *AttributeStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *AttributeStore) Create(ctx context.Context, m *Attribute) error {
	return s.DB.WithContext(ctx).Create(m).Error
}

// List returns the Attribute records matching opts.
func (s *AttributeStore) List(ctx context.Context, opts gormx.ListOptions) ([]Attribute, error) {
	var out []Attribute
	if err := opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Attribute records matching opts.Where
// (pagination and ordering are ignored).
func (s *AttributeStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	db := s.DB.WithContext(ctx).Model(&Attribute{})
	if opts.Where != nil {
		db = db.Where(opts.Where, opts.Args...)
	}
	if err := db.Count(&n).Error; err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *AttributeStore) Update(ctx context.Context, m *Attribute) error {
	return s.DB.WithContext(ctx).Save(m).Error
}

// GetByID fetches the Attribute with the given primary key.
func (s *AttributeStore) GetByID(ctx context.Context, id string) (*Attribute, error) {
	var m Attribute
	if err := s.DB.WithContext(ctx).First(&m, "id = ?", id).Error; err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Attribute with the given primary key.
func (s *AttributeStore) DeleteByID(ctx context.Context, id string) error {
	return s.DB.WithContext(ctx).Delete(&Attribute{}, "id = ?", id).Error
}

// ListByUserID returns the Attribute records whose user_id matches id, with opts applied.
func (s *AttributeStore) ListByUserID(ctx context.Context, id string, opts gormx.ListOptions) ([]Attribute, error) {
	var out []Attribute
	q := opts.Apply(s.DB.WithContext(ctx).Where("user_id = ?", id))
	if err := q.Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}
