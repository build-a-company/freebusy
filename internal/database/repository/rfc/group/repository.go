// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: protobuf/rfc4519/group/v1/ldap_group.proto, protobuf/rfc7643/group/v1/group.proto
//
// database: rfc
// schema:   group
//
// Proto-facing repository interfaces; adapters compose the generated gorm and GraphQL outputs.
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

package group

import (
	"context"
	"github.com/oh-tarnished/freebusy/internal/database/gorm/filterx"
	"github.com/oh-tarnished/freebusy/internal/database/hasura/freebusyql"
	"github.com/oh-tarnished/freebusy/internal/database/repository/repox"
	"github.com/oh-tarnished/freebusy/protobuf/generated/go/protobuf/rfc4519/group/v1/ldapgrouppbv1"
	"github.com/oh-tarnished/freebusy/protobuf/generated/go/protobuf/rfc7643/group/v1/scimgrouppbv1"
	"gorm.io/gorm"
)

// GroupRepository is the provider-agnostic persistence surface for
// Group (groups/{group}): full-message writes, AIP-160/AIP-132 lists with
// opaque page tokens, field-mask updates, and etag optimistic concurrency.
type GroupRepository interface {
	// Create persists m and returns the stored record.
	Create(ctx context.Context, m *scimgrouppbv1.Group) (*scimgrouppbv1.Group, error)
	// Get returns the record addressed by its resource name, or
	// repox.ErrNotFound.
	Get(ctx context.Context, name string) (*scimgrouppbv1.Group, error)
	// List returns one page of records.
	List(ctx context.Context, in repox.ListInput) ([]*scimgrouppbv1.Group, string, error)
	// Update persists the masked fields of m; an empty mask replaces every
	// mutable field. m.Etag, when set, guards against concurrent writes
	// (repox.ErrConflict).
	Update(ctx context.Context, m *scimgrouppbv1.Group, paths []string) (*scimgrouppbv1.Group, error)
	// Delete removes the record addressed by its resource name.
	Delete(ctx context.Context, name string) error
}

// GroupHooks lets custom logic run inside the generated Group
// adapters without editing them: normalization/derivation before writes,
// derived fields after reads, and guards before deletes. Nil funcs are skipped.
type GroupHooks struct {
	// BeforeCreate runs after name/id resolution, before the row is written.
	BeforeCreate func(ctx context.Context, m *scimgrouppbv1.Group) error
	// AfterRead runs on every record a read returns (Get, List, and the
	// re-reads writes return).
	AfterRead func(ctx context.Context, m *scimgrouppbv1.Group) error
	// BeforeUpdate runs after the mask is applied to the merged record,
	// before it is written.
	BeforeUpdate func(ctx context.Context, existing, merged *scimgrouppbv1.Group, paths []string) error
	// BeforeDelete runs before the row is removed; returning an error vetoes.
	BeforeDelete func(ctx context.Context, name string) error
}

// LdapGroupRepository is the provider-agnostic persistence surface for
// LdapGroup (ldapGroups/{ldap_group}): full-message writes, AIP-160/AIP-132 lists with
// opaque page tokens, field-mask updates, and etag optimistic concurrency.
type LdapGroupRepository interface {
	// Create persists m and returns the stored record.
	Create(ctx context.Context, m *ldapgrouppbv1.LdapGroup) (*ldapgrouppbv1.LdapGroup, error)
	// Get returns the record addressed by its resource name, or
	// repox.ErrNotFound.
	Get(ctx context.Context, name string) (*ldapgrouppbv1.LdapGroup, error)
	// List returns one page of records.
	List(ctx context.Context, in repox.ListInput) ([]*ldapgrouppbv1.LdapGroup, string, error)
	// Update persists the masked fields of m; an empty mask replaces every
	// mutable field. m.Etag, when set, guards against concurrent writes
	// (repox.ErrConflict).
	Update(ctx context.Context, m *ldapgrouppbv1.LdapGroup, paths []string) (*ldapgrouppbv1.LdapGroup, error)
	// Delete removes the record addressed by its resource name.
	Delete(ctx context.Context, name string) error
}

// LdapGroupHooks lets custom logic run inside the generated LdapGroup
// adapters without editing them: normalization/derivation before writes,
// derived fields after reads, and guards before deletes. Nil funcs are skipped.
type LdapGroupHooks struct {
	// BeforeCreate runs after name/id resolution, before the row is written.
	BeforeCreate func(ctx context.Context, m *ldapgrouppbv1.LdapGroup) error
	// AfterRead runs on every record a read returns (Get, List, and the
	// re-reads writes return).
	AfterRead func(ctx context.Context, m *ldapgrouppbv1.LdapGroup) error
	// BeforeUpdate runs after the mask is applied to the merged record,
	// before it is written.
	BeforeUpdate func(ctx context.Context, existing, merged *ldapgrouppbv1.LdapGroup, paths []string) error
	// BeforeDelete runs before the row is removed; returning an error vetoes.
	BeforeDelete func(ctx context.Context, name string) error
}

// Repositories bundles this schema's repository surfaces behind one factory.
type Repositories struct {
	Groups     GroupRepository
	LdapGroups LdapGroupRepository
}

// options collects the per-resource customizations the factories thread into
// the adapters they build.
type options struct {
	groupHooks            GroupHooks
	groupOverrides        map[string]filterx.SQLHandler
	ldapGroupHooks        LdapGroupHooks
	ldapGroupOverrides    map[string]filterx.SQLHandler
	groupGQLOverrides     map[string]filterx.GraphQLHandler
	ldapGroupGQLOverrides map[string]filterx.GraphQLHandler
}

// Option customizes the adapters a factory builds.
type Option func(*options)

// WithGroupHooks installs h on the Group adapters.
func WithGroupHooks(h GroupHooks) Option {
	return func(o *options) { o.groupHooks = h }
}

// WithGroupListOverride substitutes the generated filter dispatch for one
// Group filter field (e.g. a derived state computed via subqueries).
func WithGroupListOverride(field string, h filterx.SQLHandler) Option {
	return func(o *options) {
		if o.groupOverrides == nil {
			o.groupOverrides = map[string]filterx.SQLHandler{}
		}
		o.groupOverrides[field] = h
	}
}

// WithLdapGroupHooks installs h on the LdapGroup adapters.
func WithLdapGroupHooks(h LdapGroupHooks) Option {
	return func(o *options) { o.ldapGroupHooks = h }
}

// WithLdapGroupListOverride substitutes the generated filter dispatch for one
// LdapGroup filter field (e.g. a derived state computed via subqueries).
func WithLdapGroupListOverride(field string, h filterx.SQLHandler) Option {
	return func(o *options) {
		if o.ldapGroupOverrides == nil {
			o.ldapGroupOverrides = map[string]filterx.SQLHandler{}
		}
		o.ldapGroupOverrides[field] = h
	}
}

// WithGroupGraphQLListOverride substitutes the generated filter dispatch
// for one Group filter field on the GraphQL adapters.
func WithGroupGraphQLListOverride(field string, h filterx.GraphQLHandler) Option {
	return func(o *options) {
		if o.groupGQLOverrides == nil {
			o.groupGQLOverrides = map[string]filterx.GraphQLHandler{}
		}
		o.groupGQLOverrides[field] = h
	}
}

// WithLdapGroupGraphQLListOverride substitutes the generated filter dispatch
// for one LdapGroup filter field on the GraphQL adapters.
func WithLdapGroupGraphQLListOverride(field string, h filterx.GraphQLHandler) Option {
	return func(o *options) {
		if o.ldapGroupGQLOverrides == nil {
			o.ldapGroupGQLOverrides = map[string]filterx.GraphQLHandler{}
		}
		o.ldapGroupGQLOverrides[field] = h
	}
}

// New picks the adapter set for the live handle in conn: GraphQL when a
// client connection is present, otherwise GORM.
func New(conn repox.Conn, opts ...Option) Repositories {
	if conn.GraphQL != nil {
		return NewGraphQL(conn.GraphQL, opts...)
	}
	return NewGorm(conn.Gorm, opts...)
}

// NewGraphQL builds the GraphQL adapter set over svc.
func NewGraphQL(svc *freebusyql.Service, opts ...Option) Repositories {
	var o options
	for _, opt := range opts {
		opt(&o)
	}
	return Repositories{
		Groups:     &GraphQLGroupRepository{Svc: svc, Hooks: o.groupHooks, ListOverrides: o.groupGQLOverrides},
		LdapGroups: &GraphQLLdapGroupRepository{Svc: svc, Hooks: o.ldapGroupHooks, ListOverrides: o.ldapGroupGQLOverrides},
	}
}

// NewGorm builds the GORM adapter set over db.
func NewGorm(db *gorm.DB, opts ...Option) Repositories {
	var o options
	for _, opt := range opts {
		opt(&o)
	}
	return Repositories{
		Groups:     &GormGroupRepository{DB: db, Hooks: o.groupHooks, ListOverrides: o.groupOverrides},
		LdapGroups: &GormLdapGroupRepository{DB: db, Hooks: o.ldapGroupHooks, ListOverrides: o.ldapGroupOverrides},
	}
}
