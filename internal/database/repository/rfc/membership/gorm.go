// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: protobuf/rfc4519/membership/v1/membership.proto
//
// database: rfc
// schema:   membership
//
// GORM adapters composing the generated models, stores, converters, and filterx specs.
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

package membership

import (
	"context"
	"fmt"
	"github.com/oh-tarnished/freebusy/internal/database/gorm/filterx"
	"github.com/oh-tarnished/freebusy/internal/database/gorm/rfc/membership"
	"github.com/oh-tarnished/freebusy/internal/database/repository/repox"
	"github.com/oh-tarnished/freebusy/protobuf/generated/go/protobuf/rfc4519/membership/v1/membershippbv1"
	"google.golang.org/protobuf/proto"
	"gorm.io/gorm"
)

// GormMembershipRepository is the GORM adapter of MembershipRepository: flat
// CRUD over the generated model, store, converters, and filterx spec. Exported
// fields and helpers are the Tier-2 seam — embed it and override whole methods
// for behavior the schema cannot express; generated methods only call private
// helpers, never the public interface, so overrides never re-enter generated
// code.
type GormMembershipRepository struct {
	DB    *gorm.DB
	Hooks MembershipHooks
	// ListOverrides substitute the generated dispatch for single filter fields
	// (e.g. a derived state computed via subqueries).
	ListOverrides map[string]filterx.SQLHandler
}

// NewGormMembershipRepository returns the GORM adapter bound to db.
func NewGormMembershipRepository(db *gorm.DB) *GormMembershipRepository {
	return &GormMembershipRepository{DB: db}
}

// Create persists in under parent and returns the stored record.
func (r *GormMembershipRepository) Create(ctx context.Context, parent string, in *membershippbv1.Membership) (*membershippbv1.Membership, error) {
	parentIDs, err := repox.SplitName(parent, "ldapGroups")
	if err != nil {
		return nil, err
	}
	id := repox.NewULID()
	if in.GetName() != "" {
		ids, err := repox.SplitName(in.GetName(), "ldapGroups", "memberships")
		if err != nil {
			return nil, err
		}
		for i := range parentIDs {
			if ids[i] != parentIDs[i] {
				return nil, fmt.Errorf("%w: name %q is not under parent %q", repox.ErrInvalidArgument, in.GetName(), parent)
			}
		}
		id = ids[len(ids)-1]
	}
	in = proto.Clone(in).(*membershippbv1.Membership)
	in.Name = FormatMembershipName(parentIDs[0], id)
	if h := r.Hooks.BeforeCreate; h != nil {
		if err := h(ctx, in); err != nil {
			return nil, err
		}
	}
	m := membership.MembershipFromProto(in)
	m.ID = id
	m.Name = in.GetName()
	m.LdapGroupID = parentIDs[0]
	m.Etag = repox.Ptr(repox.NewULID())
	if err := membership.NewMembershipStore(r.DB).Create(ctx, m); err != nil {
		return nil, repox.MapGormErr(err)
	}
	return r.get(ctx, id)
}

// Get returns the record addressed by its resource name.
func (r *GormMembershipRepository) Get(ctx context.Context, name string) (*membershippbv1.Membership, error) {
	ids, err := repox.SplitName(name, "ldapGroups", "memberships")
	if err != nil {
		return nil, err
	}
	return r.get(ctx, ids[len(ids)-1])
}

// get loads by surrogate key — the private read every generated method re-reads
// through, so Tier-2 overrides of Get never re-enter generated writes.
func (r *GormMembershipRepository) get(ctx context.Context, id string) (*membershippbv1.Membership, error) {
	var m membership.Membership
	if err := r.DB.WithContext(ctx).First(&m, "id = ?", id).Error; err != nil {
		return nil, repox.MapGormErr(err)
	}
	return r.toProto(ctx, &m)
}

// toProto converts a loaded row, decorating reference names and running the
// AfterRead hook.
func (r *GormMembershipRepository) toProto(ctx context.Context, m *membership.Membership) (*membershippbv1.Membership, error) {
	out := membership.MembershipToProto(m)
	if h := r.Hooks.AfterRead; h != nil {
		if err := h(ctx, out); err != nil {
			return nil, err
		}
	}
	return out, nil
}

// List returns one page of records under parent.
func (r *GormMembershipRepository) List(ctx context.Context, parent string, in repox.ListInput) ([]*membershippbv1.Membership, string, error) {
	parentIDs, err := repox.SplitName(parent, "ldapGroups")
	if err != nil {
		return nil, "", err
	}
	scope := r.DB.Where("ldap_group_id = ?", parentIDs[len(parentIDs)-1])
	return r.list(ctx, scope, in)
}

func (r *GormMembershipRepository) list(ctx context.Context, scope *gorm.DB, in repox.ListInput) ([]*membershippbv1.Membership, string, error) {
	conds, err := filterx.Parse(in.Filter)
	if err != nil {
		return nil, "", repox.MapFilterxErr(err)
	}
	eng := filterx.Gorm[membership.Membership](membership.MembershipFilterSpec)
	for f, h := range r.ListOverrides {
		eng.Override(f, h)
	}
	rows, next, err := eng.List(ctx, scope, filterx.ListInput{
		PageSize:  in.PageSize,
		PageToken: in.PageToken,
		OrderBy:   in.OrderBy,
		Filter:    conds,
	})
	if err != nil {
		return nil, "", repox.MapFilterxErr(err)
	}
	items := make([]*membershippbv1.Membership, 0, len(rows))
	for i := range rows {
		out, err := r.toProto(ctx, &rows[i])
		if err != nil {
			return nil, "", err
		}
		items = append(items, out)
	}
	return items, next, nil
}

// Update persists the masked fields of in; an empty mask replaces every
// mutable field. The write happens in one transaction guarded by in.Etag.
func (r *GormMembershipRepository) Update(ctx context.Context, in *membershippbv1.Membership, paths []string) (*membershippbv1.Membership, error) {
	ids, err := repox.SplitName(in.GetName(), "ldapGroups", "memberships")
	if err != nil {
		return nil, err
	}
	id := ids[len(ids)-1]
	err = r.DB.Transaction(func(tx *gorm.DB) error {
		var existing membership.Membership
		if err := tx.WithContext(ctx).First(&existing, "id = ?", id).Error; err != nil {
			return err
		}
		if in.GetEtag() != "" && existing.Etag != nil && *existing.Etag != in.GetEtag() {
			return repox.ErrConflict
		}
		existingPB := membership.MembershipToProto(&existing)
		merged := proto.Clone(existingPB).(*membershippbv1.Membership)
		applyMembershipMask(merged, in, paths)
		if h := r.Hooks.BeforeUpdate; h != nil {
			if err := h(ctx, existingPB, merged, paths); err != nil {
				return err
			}
		}
		next := membership.MembershipFromProto(merged)
		_ = next
		existing.Name = next.Name
		existing.Member = next.Member
		existing.Role = next.Role
		existing.Etag = repox.Ptr(repox.NewULID())
		return membership.NewMembershipStore(tx).Update(ctx, &existing)
	})
	if err != nil {
		return nil, repox.MapGormErr(err)
	}
	return r.get(ctx, id)
}

// Delete removes the record addressed by its resource name.
func (r *GormMembershipRepository) Delete(ctx context.Context, name string) error {
	ids, err := repox.SplitName(name, "ldapGroups", "memberships")
	if err != nil {
		return err
	}
	if h := r.Hooks.BeforeDelete; h != nil {
		if err := h(ctx, name); err != nil {
			return err
		}
	}
	id := ids[len(ids)-1]
	var existing membership.Membership
	if err := r.DB.WithContext(ctx).First(&existing, "id = ?", id).Error; err != nil {
		return repox.MapGormErr(err)
	}
	return repox.MapGormErr(membership.NewMembershipStore(r.DB).DeleteByID(ctx, id))
}

// Compile-time proof the adapter satisfies the interface.
var _ MembershipRepository = (*GormMembershipRepository)(nil)
