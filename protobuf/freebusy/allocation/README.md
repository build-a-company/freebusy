# Allocation Service

**Package:** `freebusy.allocation.v1`

> [!IMPORTANT]
> Auto-generated by `protodoc`. Do not edit manually.

## Services

### AllocationService

AllocationService is the read-only half of the engine: it answers how much is free, and never changes anything. HoldService is the half that takes.  The counting is done here rather than stored anywhere. No RFC models an inventory count -- RFC 5545 and RFC 7953 are one-calendar-one-timeline -- so "5 of 12 free" is produced by asking each RFC 9073 resource's calendar whether it is free across the window and totalling the answers. RFC 7953's VAVAILABILITY says when a resource was on offer at all; bookings on its calendar say what has since been taken; the difference is availability.  Every operation is a custom method (AIP-136): they compute rather than fetch, so they take neither the standard-method names nor their shapes.

| Method | Request | Response | Description |
| --- | --- | --- | --- |
| `ComputeAvailability` | `ComputeAvailabilityRequest` | `ComputeAvailabilityResponse` | Computes availability across a site's resources, bucketed by slot or day. |
| `CheckAvailability` | `CheckAvailabilityRequest` | `CheckAvailabilityResponse` | Checks one exact window for one exact quantity, with reasons when it fails.  Advisory, not a reservation: the answer can be stale the instant it is returned. CreateHold is what actually secures anything. |
| `BatchComputeAvailability` | `BatchComputeAvailabilityRequest` | `BatchComputeAvailabilityResponse` | Runs several availability computations in one round trip.  Each entry is independent and may name a different site, window or granularity. Capped at 50 entries, because each one fans out across every candidate resource's calendar. |
| `SearchAvailability` | `SearchAvailabilityRequest` | `SearchAvailabilityResponse` | Sweeps an operator for resources free across a window.  The search entry point, where ComputeAvailability answers for a site the caller has already chosen. Paginated, since a large operator may match thousands of resources. |

### HoldService

HoldService takes and releases expiring claims on resources. It is the only thing in this API that mutates availability.  Two things are deliberately not RPCs, following the precedent SchedulingService set: the sweeper that expires lapsed holds is an internal job, not a call, and conversion to a confirmed booking is driven from the booking side, so a hold is never converted by a caller reaching in here.  There is no UpdateHold. A hold's window and resources are immutable by construction -- changing either is a different allocation -- so the only mutations are the two custom methods below.

| Method | Request | Response | Description |
| --- | --- | --- | --- |
| `CreateHold` | `CreateHoldRequest` | `Hold` | Takes a hold, allocating concrete resources for the window.  The server chooses which resources are taken from the candidates offered, under a lock, so two callers racing for the last bay cannot both win. Requires an idempotency key: a retry after a timeout returns the original hold rather than taking a second one. |
| `GetHold` | `GetHoldRequest` | `Hold` | Retrieves one hold.  Authoritative on expiry: a hold whose expire_time has passed is reported EXPIRED here even if the sweeper has not yet written that state, so a caller never sees an ACTIVE hold that no longer reserves anything. |
| `ListHolds` | `ListHoldsRequest` | `ListHoldsResponse` | Lists the holds taken at a site. |
| `ExtendHold` | `ExtendHoldRequest` | `Hold` | Pushes a hold's expiry further out.  Measured from now rather than from the current deadline, so repeated calls cannot accumulate an unbounded reservation. Fails on a hold that is not ACTIVE -- an expired hold is gone, not extendable. |
| `ReleaseHold` | `ReleaseHoldRequest` | `Hold` | Gives a hold up before it expires, freeing its resources immediately.  Idempotent: releasing an already-released or expired hold succeeds and changes nothing, because the caller's intent is already satisfied. |

## Messages

### Hold

A Hold is a temporary, expiring claim on one or more bookable resources for a window, taken before payment or confirmation so two callers cannot be sold the same thing.  Native, with no RFC behind it, and the comment says why (the same trade protobuf.shared.project.v1 records upstream). RFC 5545 section 3.2.12's PARTSTAT=TENTATIVE can express that a claim is provisional, and RFC 5546's iTIP can carry the request that created it, but neither specifies a lapse time, and an expiring claim is the one thing a commercial booking engine cannot do without. The resources held are RFC 9073 VRESOURCEs and the window is measured against their RFC 7953 availability; only the clock is ours.  Holds are allocated, not requested: CreateHold names candidate resources and a quantity, and the server decides which concrete resources are taken. That keeps "which bay did I get" a server decision made under a lock, rather than a client guess that races.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `IDENTIFIER` | The hold name. Nested under the site whose resources it takes, so a hold is listed and swept per site without joining through every resource. Format: organizationalUnits/{organizational_unit}/holds/{hold} |
| `resources` | `repeated string` | `OUTPUT_ONLY` | The concrete resources this hold has taken, one per unit of `quantity`. Chosen by the server from the candidates the request offered. Format: resources/{resource} |
| `window` | `Interval` | `REQUIRED` | The window held. Immutable: moving it is a different allocation, so release this hold and take another. |
| `candidate_resources` | `repeated string` | `OPTIONAL` | Candidate resources the allocator may choose from, supplied on create. Empty offers every resource the site holds. Immutable: the choice is made once, and re-offering a different set is a different allocation. Format: resources/{resource} |
| `quantity` | `int32` | `OPTIONAL` | How many resources to take, supplied on create; defaults to 1. Once the hold is ACTIVE this equals the length of `resources`. |
| `expire_time` | `Timestamp` | `OUTPUT_ONLY` | When the hold lapses if nothing confirms it. Extend it with ExtendHold; past this instant the hold no longer reserves anything, whether or not the sweeper has yet marked it EXPIRED. |
| `state` | `HoldState` | `OUTPUT_ONLY` | Lifecycle state. New holds start ACTIVE. |
| `idempotency_key` | `string` | `OPTIONAL` | Caller-supplied key making CreateHold safe to retry. A repeat with the same key returns the original hold rather than taking a second one. |
| `create_time` | `Timestamp` | `OUTPUT_ONLY` | Creation timestamp. |
| `update_time` | `Timestamp` | `OUTPUT_ONLY` | Last-modification timestamp. |
| `etag` | `string` | `OUTPUT_ONLY` | Opaque version for optimistic concurrency (AIP-154); echo on update/delete. |

### AvailabilityBucket

One slice of an availability answer: how many resources are free across a span, and which ones.  This is where pooling happens. No RFC counts inventory -- RFC 5545 and 7953 are one-calendar-one-timeline -- so a count is produced by asking each resource's calendar whether it is free and totalling the answers, rather than by storing a capacity number anywhere.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `window` | `Interval` | - | The span this bucket covers: one slot, or one calendar day in the target time zone, per the request's granularity. |
| `free_count` | `int32` | - | How many candidate resources are free across the whole span. |
| `total_count` | `int32` | - | How many candidates were considered. `free_count` out of this is the "5 of 12 free" a caller renders. |
| `free_resources` | `repeated string` | - | Which resources are free, when the request asked for them. Omitted by default: a car park with 200 bays would otherwise repeat 200 names in every bucket of every day. Format: resources/{resource} |

### UnbookableReason

Why a requested window is not bookable.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `code` | `UnbookableReasonCode` | - | What went wrong, as a code a caller can branch on. |
| `message` | `string` | - | Human-readable detail for logs and support, not for parsing. |
| `resource` | `string` | - | The resource this reason is about, when it concerns one in particular. Format: resources/{resource} |

### AvailabilityMatch

One resource that satisfies a search, with the window it is free for.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `resource` | `string` | - | The free resource. Format: resources/{resource} |
| `organizational_unit` | `string` | - | The site it belongs to. Format: organizationalUnits/{organizational_unit} |
| `window` | `Interval` | - | The span it is free for, which may be narrower than the searched window. |
| `resource_types` | `repeated string` | - | RESOURCE-TYPE values carried by the resource, RFC 9073 section 6.3, echoed so a caller can group results without re-fetching each resource. |

### ComputeAvailabilityRequest

Request message for AllocationService.ComputeAvailability.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | `REQUIRED` | The site whose resources to compute over. Format: organizationalUnits/{organizational_unit} |
| `resources` | `repeated string` | `OPTIONAL` | Restrict the computation to these resources. Empty means every resource the site holds. Format: resources/{resource} |
| `window` | `Interval` | `REQUIRED` | The window to compute over. |
| `granularity` | `AvailabilityGranularity` | `OPTIONAL` | How to slice the window. Defaults by window length. |
| `slot_duration` | `Duration` | `OPTIONAL` | Slot length for SLOT granularity. Ignored for DAY. |
| `time_zone` | `string` | `OPTIONAL` | IANA time zone the day boundaries are drawn in, e.g. "Asia/Kolkata". Defaults to the site's zone. Required in effect for DAY granularity, since "a night" has no meaning without one. |
| `include_free_resources` | `bool` | `OPTIONAL` | Populate AvailabilityBucket.free_resources. Off by default: a large site would otherwise repeat every resource name in every bucket. |

### ComputeAvailabilityResponse

Response message for AllocationService.ComputeAvailability.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `buckets` | `repeated AvailabilityBucket` | - | One bucket per slot or day, in ascending time order. |
| `total_count` | `int32` | - | How many resources were considered across the whole window. |

### CheckAvailabilityRequest

Request message for AllocationService.CheckAvailability.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | `REQUIRED` | The site whose resources to check. Format: organizationalUnits/{organizational_unit} |
| `resources` | `repeated string` | `OPTIONAL` | Restrict the check to these resources. Empty means every resource the site holds. Format: resources/{resource} |
| `window` | `Interval` | `REQUIRED` | The exact window to check. |
| `quantity` | `int32` | `OPTIONAL` | How many resources are needed. Defaults to 1. |

### CheckAvailabilityResponse

Response message for AllocationService.CheckAvailability.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `bookable` | `bool` | - | Whether `quantity` resources can be held for the window right now. |
| `free_count` | `int32` | - | How many are actually free, which may be fewer than requested. |
| `reasons` | `repeated UnbookableReason` | - | Why not, when bookable is false. Empty when it is true. |
| `candidate_resources` | `repeated string` | - | Which resources would be taken, in the order the allocator would pick them. Advisory: another caller may take one before a hold is created. Format: resources/{resource} |

### BatchComputeAvailabilityRequest

Request message for AllocationService.BatchComputeAvailability. Each entry is a full ComputeAvailabilityRequest, so sites, windows and granularities may differ within one batch.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `requests` | `repeated ComputeAvailabilityRequest` | `REQUIRED` | The computations to run. Capped at 50 to bound the calendar reads a single call can trigger. |

### BatchComputeAvailabilityResponse

Response message for AllocationService.BatchComputeAvailability.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `responses` | `repeated ComputeAvailabilityResponse` | - | One response per request, in the order requested. |

### SearchAvailabilityRequest

Request message for AllocationService.SearchAvailability. Sweeps a whole operator for resources free across a window, where ComputeAvailability answers for one site already chosen.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | `REQUIRED` | The operator to search within. Format: organizations/{organization} |
| `window` | `Interval` | `REQUIRED` | The window that must be free. |
| `organizational_units` | `repeated string` | `OPTIONAL` | Restrict to sites, e.g. one car park or one hotel. Empty searches all. Format: organizationalUnits/{organizational_unit} |
| `resource_types` | `repeated string` | `OPTIONAL` | Restrict to RESOURCE-TYPE values, RFC 9073 section 6.3, e.g. "room" or "parking-bay". Empty matches every type. |
| `quantity` | `int32` | `OPTIONAL` | How many resources must be free together at one site for it to match. Defaults to 1. |
| `page_size` | `int32` | `OPTIONAL` | Maximum matches to return. Defaults to 50, capped at 1000. |
| `page_token` | `string` | `OPTIONAL` | Page token from a previous SearchAvailabilityResponse.next_page_token. |

### SearchAvailabilityResponse

Response message for AllocationService.SearchAvailability.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `matches` | `repeated AvailabilityMatch` | - | The free resources found. |
| `next_page_token` | `string` | - | Token for the next page, empty when this is the last page. |

### CreateHoldRequest

Request message for HoldService.CreateHold, AIP-133.  The allocating call, in standard-method clothing: the caller states what it wants on the Hold -- window, quantity, and which resources it will accept -- and the server picks the concrete resources under a lock. There is deliberately no separate Allocate method, so there is one place where the race is handled.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | `REQUIRED` | The site whose resources may be taken. Format: organizationalUnits/{organizational_unit} |
| `hold` | `Hold` | `REQUIRED` | The hold to take. Its window is required; quantity defaults to 1 and candidate_resources empty offers every resource the site holds. Supply idempotency_key to make a retry safe. |
| `hold_id` | `string` | `OPTIONAL` | Client-assigned id, becoming the last segment of the hold name. The server generates one when empty. |

### GetHoldRequest

Request message for HoldService.GetHold, AIP-131.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the hold to retrieve. Format: organizationalUnits/{organizational_unit}/holds/{hold} |

### ListHoldsRequest

Request message for HoldService.ListHolds, AIP-132.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | `REQUIRED` | The site whose holds to list. Format: organizationalUnits/{organizational_unit} |
| `page_size` | `int32` | `OPTIONAL` | Maximum holds to return. Defaults to 50, capped at 1000. |
| `page_token` | `string` | `OPTIONAL` | Page token from a previous ListHoldsResponse.next_page_token. |
| `filter` | `string` | `OPTIONAL` | Filter expression, AIP-160, over the hold's own fields, e.g. `state=HOLD_STATE_ACTIVE`. |

### ListHoldsResponse

Response message for HoldService.ListHolds, AIP-132.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `holds` | `repeated Hold` | - | The holds on this page. |
| `next_page_token` | `string` | - | Token for the next page, empty when this is the last page. |

### ExtendHoldRequest

Request message for HoldService.ExtendHold.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the hold to extend. Format: organizationalUnits/{organizational_unit}/holds/{hold} |
| `duration` | `Duration` | `REQUIRED` | How much further out to push expire_time, measured from now rather than from the current deadline, so repeated extensions cannot accumulate. |
| `etag` | `string` | `OPTIONAL` | Opaque version read from the hold; the extension is rejected if the hold changed since (AIP-154). |

### ReleaseHoldRequest

Request message for HoldService.ReleaseHold.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the hold to release. Format: organizationalUnits/{organizational_unit}/holds/{hold} |
| `etag` | `string` | `OPTIONAL` | Opaque version read from the hold; the release is rejected if the hold changed since (AIP-154). |

### DeleteHoldRequest

Request message for HoldService.DeleteHold, AIP-135.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the hold to delete. Format: organizationalUnits/{organizational_unit}/holds/{hold} |
| `etag` | `string` | `OPTIONAL` | Opaque version read from the hold; the delete is rejected if the hold changed since (AIP-154). |

## Enums

### HoldState

Lifecycle of a Hold. A hold is the only thing in this API that expires on its own: RFC 5545 section 3.2.12's PARTSTAT=TENTATIVE says a tentative claim exists, but no RFC says when it lapses, so the clock is ours.

| Value | Number | Description |
| --- | --- | --- |
| `HOLD_STATE_UNSPECIFIED` | 0 | Default; never persisted. |
| `HOLD_STATE_ACTIVE` | 1 | Live and holding its resources against other callers, until expire_time. |
| `HOLD_STATE_RELEASED` | 2 | Given up before expiry, by ReleaseHold or by the booking it backed failing. The resources are free again. |
| `HOLD_STATE_EXPIRED` | 3 | expire_time passed with no confirmation. Reached by the sweeper, and by any authoritative read that notices the deadline before the sweeper does, so a stale ACTIVE row never hands out a resource twice. |
| `HOLD_STATE_CONVERTED` | 4 | Converted into a confirmed booking. Terminal, and the only outcome that keeps the resources allocated. |

### AvailabilityGranularity

The slicing of an availability answer over a window.

| Value | Number | Description |
| --- | --- | --- |
| `AVAILABILITY_GRANULARITY_UNSPECIFIED` | 0 | Default; the server picks DAY for windows longer than 48 hours and SLOT below that. |
| `AVAILABILITY_GRANULARITY_SLOT` | 1 | One bucket per slot of slot_duration, for hourly bookings: meeting rooms, courts, equipment. |
| `AVAILABILITY_GRANULARITY_DAY` | 2 | One bucket per calendar day in the target time zone, for overnight bookings: hotel rooms, daily parking. |

### UnbookableReasonCode

Why a window could not be booked. Returned by CheckAvailability so a caller can say what to change rather than only that it failed.

| Value | Number | Description |
| --- | --- | --- |
| `UNBOOKABLE_REASON_CODE_UNSPECIFIED` | 0 | Default; never returned. |
| `UNBOOKABLE_REASON_CODE_NO_CAPACITY` | 1 | Fewer free resources than the requested quantity. |
| `UNBOOKABLE_REASON_CODE_OUTSIDE_AVAILABILITY` | 2 | The window falls outside every AvailablePeriod of the resource's RFC 7953 availability pattern -- the time was never on offer. |
| `UNBOOKABLE_REASON_CODE_HELD` | 3 | A live hold covers the window. |
| `UNBOOKABLE_REASON_CODE_BOOKED` | 4 | A confirmed booking covers the window. |
| `UNBOOKABLE_REASON_CODE_WINDOW_TOO_SHORT` | 5 | Shorter than the minimum stay or slot the resource accepts. |
| `UNBOOKABLE_REASON_CODE_WINDOW_TOO_LONG` | 6 | Longer than the maximum stay the resource accepts. |
| `UNBOOKABLE_REASON_CODE_LEAD_TIME` | 7 | Inside the resource's notice period, or beyond how far ahead it opens. |
| `UNBOOKABLE_REASON_CODE_RESOURCE_INACTIVE` | 8 | The resource is soft-deleted or otherwise not accepting bookings. |

---

© 2026 oh-tarnished | Apache 2.0 License
