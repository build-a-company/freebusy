// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: protobuf/rfc9553/card/v1/card.proto
//
// database: rfc
// schemas:  card
//
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

/// Card is the JSContact Card object, RFC 9553 <https://www.rfc-editor.org/rfc/rfc9553.html> as amended by RFC 9982 <https://www.rfc-editor.org/rfc/rfc9982.html>. **The canonical contact model** under rule 14: what this schema stores. protobuf.rfc6350.vcard.v1 remains a first-class package with its own CRUD, but it is the legacy representation -- a vCard saved through that API is converted here for storage and converted back on read, per RFC 9555. Most multi-valued properties are **maps keyed by a stable id**, not lists. That is section 1.4.1's `Id` type and it is load-bearing: the key must survive across versions of the same Card, because localizations and patches address individual entries by it. A list would renumber on every edit and break both. RFC 9982 makes `jscontact_uid` optional, where RFC 9553 required it -- the later reading is the one implemented, because auto-generating an ephemeral uid during vCard conversion is exactly what 9982 set out to stop. Reference: RFC 9553 "JSContact: A JSON Representation of Contact Data". https://www.rfc-editor.org/rfc/rfc9553.html Reference: RFC 9982 "JSContact Version 2.0". https://www.rfc-editor.org/rfc/rfc9982.html
model Card {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name, "cards/{card}". Assigned by the server.
  name String @unique @map("name")

  /// Server-assigned unique id.
  uid String? @unique @map("uid")

  /// The Card's own "uid", RFC 9553 section 2.1.9 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.1.9>, made optional by RFC 9982 <https://www.rfc-editor.org/rfc/rfc9982.html>. Interchange identity, distinct from `uid` above: it arrives inside an imported Card, is whatever the originating system chose, and must survive a round trip unchanged or every re-import duplicates the address book.
  jscontactUid String? @map("jscontact_uid")

  /// "kind", section 2.1.4 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.1.4>. Absent means an individual.
  kind CardKind? @map("kind")

  /// "nicknames", section 2.2.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.2>.
  nicknames Json? @map("nicknames")

  /// "organizations", section 2.2.3 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.3>.
  organizations Json? @map("organizations")

  /// "titles", section 2.2.5 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.5>. Holds both vCard's TITLE and ROLE.
  titles Json? @map("titles")

  /// "emails", section 2.3.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.3.1>.
  emails Json? @map("emails")

  /// "onlineServices", section 2.3.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.3.2>. Holds both vCard's IMPP and RFC 9554's SOCIALPROFILE.
  onlineServices Json? @map("online_services")

  /// "phones", section 2.3.3 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.3.3>.
  phones Json? @map("phones")

  /// "preferredLanguages", section 2.3.4 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.3.4>. Which languages the contact prefers to be addressed in; `language_code` below is what this Card's own text is written in.
  preferredLanguages Json? @map("preferred_languages")

  /// "calendars", section 2.4.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.4.1>.
  calendars Json? @map("calendars")

  /// "schedulingAddresses", section 2.4.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.4.2>.
  schedulingAddresses Json? @map("scheduling_addresses")

  /// "addresses", section 2.5.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.5.1>.
  addresses Json? @map("addresses")

  /// "cryptoKeys", section 2.6.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.6.1>.
  cryptoKeys Json? @map("crypto_keys")

  /// "directories", section 2.6.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.6.2>.
  directories Json? @map("directories")

  /// "links", section 2.6.3 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.6.3>.
  links Json? @map("links")

  /// "media", section 2.6.4 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.6.4>. Holds vCard's PHOTO, SOUND and LOGO.
  media Json? @map("media")

  /// "anniversaries", section 2.8.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.8.1>. Holds vCard's BDAY and ANNIVERSARY, and a death date neither had.
  anniversaries Json? @map("anniversaries")

  /// "keywords", section 2.8.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.8.2>. vCard's CATEGORIES. A flat list, not a map: section 2.8.2 types it `String[]`, and it is the natural AIP-160 <https://aip.dev/160> filter target -- `keywords:"vip"`.
  keywords String[] @map("keywords")

  /// "notes", section 2.8.3 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.8.3>.
  notes Json? @map("notes")

  /// "personalInfo", section 2.8.4 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.8.4>.
  personalInfo Json? @map("personal_info")

  /// "relatedTo", section 2.1.8 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.1.8>, keyed by the related entity's URI or uid. Named `relations` because AIP-140 <https://aip.dev/140> bans prepositions.
  relations Json? @map("relations")

  /// "members", section 2.1.6 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.1.6>. The uids of the Cards in this group, meaningful only when `kind` is KIND_GROUP. A repeated string, not a map: section 2.1.6 types it `String[Boolean]`, a set whose values are always true, so the boolean carries nothing.
  members String[] @map("members")

  /// "language", section 2.1.5 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.1.5>. The language this Card's own human-readable text is written in, as a BCP 47 tag, RFC 5646 <https://www.rfc-editor.org/rfc/rfc5646.html>.
  languageCode String? @map("language_code")

  /// "prodId", section 2.1.7 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.1.7>. What produced this Card, e.g. a vendor and version string. AIP-140 <https://aip.dev/140> requires the abbreviation `id` over `identifier`, so the RFC's own spelling survives only in this comment.
  productID String? @map("product_id")

  /// "created", section 2.1.3 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.1.3>. When the *originating* system created the Card, distinct from `create_time` below, which is when this server first stored it. The same split as `uid` and `jscontact_uid`.
  jscontactCreateTime DateTime? @rfc.Timestamptz(6) @map("jscontact_create_time")

  /// "updated", section 2.1.10 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.1.10>. When the originating system last changed the Card.
  jscontactUpdateTime DateTime? @rfc.Timestamptz(6) @map("jscontact_update_time")

  /// Weak validator for optimistic concurrency, AIP-154 <https://aip.dev/154>. Pass it back in an update or delete to make the write conditional.
  etag String? @map("etag")

  /// When this Card was soft-deleted, unset while it is live. AIP-164 <https://aip.dev/164>.
  deleteTime DateTime? @rfc.Timestamptz(6) @map("delete_time")

  /// When a soft-deleted Card is purged for good. AIP-164 <https://aip.dev/164>.
  expireTime DateTime? @rfc.Timestamptz(6) @map("expire_time")

  /// When this Card was created here.
  createTime DateTime @default(now()) @rfc.Timestamptz(6) @map("create_time")

  /// When this Card was last modified here.
  updateTime DateTime @updatedAt @rfc.Timestamptz(6) @map("update_time")

  /// Properties this schema does not model, RFC 9553 section 1.3 <https://www.rfc-editor.org/rfc/rfc9553.html#section-1.3>, as raw JSON keyed by property name. This is also where RFC 9555 section 3 <https://www.rfc-editor.org/rfc/rfc9555.html#section-3>'s `vCardProps` lands: the vCard elements JSContact has no home for are carried here rather than dropped, which is what makes the round trip in rule 14's pipeline lossless.
  jscontactProps Json? @map("jscontact_props")

  /// Foreign key to Name.
  nameComponentsID String? @map("name_components_id")

  /// Relation to CardName via name_components_id.
  nameComponents CardName? @relation(fields: [nameComponentsID], references: [id], onDelete: SetNull)

  /// Foreign key to Speech.
  speechID String? @map("speech_id")

  /// Relation to Speech via speech_id.
  speech Speech? @relation(fields: [speechID], references: [id], onDelete: SetNull)

  /// Index on [delete_time].
  @@index([deleteTime])
  /// Index on [name_components_id].
  @@index([nameComponentsID])
  /// Index on [speech_id].
  @@index([speechID])
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([keywords], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([members], type: Gin)
  /// Maps the model to the "resource" table in the database.
  @@map("resource")
  /// Maps the model to the "card" schema in the database.
  @@schema("card")
}
