// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: protobuf/rfc9553/card/v1/name.proto
//
// database: rfc
// schemas:  card
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

/// NameComponentKind is a name component's "kind", RFC 9553 section 2.2.1.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.2>. Reference: RFC 9553 "JSContact: A JSON Representation of Contact Data", section 2.2.1.2. https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.2
enum NameComponentKind {
  /// An honorific title, e.g. "Dr".
  TITLE

  /// A given name.
  GIVEN

  /// A second given or middle name.
  GIVEN2

  /// A surname.
  SURNAME

  /// A second surname, e.g. the maternal surname in Spanish practice.
  SURNAME2

  /// A credential, e.g. "PhD".
  CREDENTIAL

  /// A generation marker, e.g. "Jr", "III".
  GENERATION

  /// Literal text to place between the components either side of it, which is why component order and `is_ordered` matter.
  SEPARATOR

  /// Maps the enum to the "name_component_kind" type in the database.
  @@map("name_component_kind")
  /// Maps the enum to the "card" schema in the database.
  @@schema("card")
}

/// Name is the Card "name" property, RFC 9553 section 2.2.1.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.1>. The structural difference from vCard is the point of JSContact. RFC 6350 section 6.2.2 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.2.2>'s N is five positional components in a fixed order, so a name that does not fit that order cannot be expressed. Here a name is an *ordered list of typed components*, so "given surname2 surname" is as representable as "surname, given" -- and `is_ordered` says whether the order given is meaningful or merely incidental. Reference: RFC 9553 "JSContact: A JSON Representation of Contact Data", section 2.2.1.1. https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.1
model CardName {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Whether `components` is in a meaningful order, section 2.2.1.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.1>. False, the default, means the order carries no information and a client should assemble the name by its own convention.
  isOrdered Boolean? @map("is_ordered")

  /// The separator to put between components when none is given explicitly, section 2.2.1.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.1>. Usually a space.
  defaultSeparator String? @map("default_separator")

  /// The whole name as a single string, section 2.2.1.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.1>. This is vCard's FN (RFC 6350 section 6.2.1 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.2.1>), and the two convert directly. Named `display_name`, not the RFC's `full`: AIP-122 <https://aip.dev/122> bans the `_name` suffix outside a short allowlist that `display_name` is on, and a bare `full` names no thing. It also matches what the vCard package calls FN, which is the property this converts to.
  displayName String? @map("display_name")

  /// How to sort this name, section 2.2.1.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.1>, keyed by the component kind to sort on -- {"surname": "Rodriguez"}. A map because sorting rules differ by component and by locale.
  sortAs Json? @map("sort_as")

  /// The script the phonetic components are written in, as an ISO 15924 four-letter code, section 2.2.1.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.1>.
  phoneticScript String? @map("phonetic_script")

  /// The notation the phonetic components use, section 2.2.1.1 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.1>.
  phoneticSystem CardPhoneticSystem? @map("phonetic_system")

  /// Back-relation: Card records that reference this model via name_components_id.
  resource Card[]

  /// Back-relation: NameComponent records that reference this model via name_id.
  nameComponents NameComponent[]

  /// Maps the model to the "names" table in the database.
  @@map("names")
  /// Maps the model to the "card" schema in the database.
  @@schema("card")
}

/// NameComponent is one typed part of a Name, RFC 9553 section 2.2.1.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.2>. Reference: RFC 9553 "JSContact: A JSON Representation of Contact Data", section 2.2.1.2. https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.2
model NameComponent {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The text of this component, section 2.2.1.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.2>. Mandatory.
  value String @map("value")

  /// What part of the name this is, section 2.2.1.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.2>. Mandatory.
  kind NameComponentKind @default(TITLE) @map("kind")

  /// A phonetic transcription of `value`, section 2.2.1.2 <https://www.rfc-editor.org/rfc/rfc9553.html#section-2.2.1.2>. The notation is on the enclosing Name, not repeated per component.
  phonetic String? @map("phonetic")

  /// Foreign key to Name.
  nameID String @map("name_id")

  /// Relation to CardName via name_id.
  name CardName @relation(fields: [nameID], references: [id], onDelete: Cascade)

  /// Index on [name_id].
  @@index([nameID])
  /// Maps the model to the "name_components" table in the database.
  @@map("name_components")
  /// Maps the model to the "card" schema in the database.
  @@schema("card")
}
