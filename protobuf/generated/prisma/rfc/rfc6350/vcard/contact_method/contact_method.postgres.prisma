// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: protobuf/rfc6350/vcard/v1/contact_method.proto
//
// database: rfc
// schemas:  vcard
//
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

/// Discriminator for the value oneof of Telephone.
enum TelephoneValueCase {
  /// The uri member is set.
  URI

  /// The text member is set.
  TEXT

  /// Maps the enum to the "telephone_value_case" type in the database.
  @@map("telephone_value_case")
  /// Maps the enum to the "vcard" schema in the database.
  @@schema("vcard")
}

/// Discriminator for the value oneof of SocialProfile.
enum SocialProfileValueCase {
  /// The uri member is set.
  URI

  /// The text member is set.
  TEXT

  /// Maps the enum to the "social_profile_value_case" type in the database.
  @@map("social_profile_value_case")
  /// Maps the enum to the "vcard" schema in the database.
  @@schema("vcard")
}

/// Email is the EMAIL property, RFC 6350 section 6.4.2 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.4.2>. Reference: RFC 6350 "vCard Format Specification", section 6.4.2. https://www.rfc-editor.org/rfc/rfc6350.html#section-6.4.2
model Email {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The address itself.
  value String @map("value")

  /// TYPE parameter, section 5.6 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.6>.
  types String? @map("types")

  /// PREF parameter, section 5.3 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.3>.
  pref Int? @map("pref")

  /// Foreign key to Contact.
  contactID String @map("contact_id")

  /// Relation to Contact via contact_id.
  contact Contact @relation(fields: [contactID], references: [id], onDelete: Cascade)

  /// Index on [contact_id].
  @@index([contactID])
  /// Maps the model to the "emails" table in the database.
  @@map("emails")
  /// Maps the model to the "vcard" schema in the database.
  @@schema("vcard")
}

/// Telephone is the TEL property, RFC 6350 section 6.4.1 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.4.1>. Reference: RFC 6350 "vCard Format Specification", section 6.4.1. https://www.rfc-editor.org/rfc/rfc6350.html#section-6.4.1
model Telephone {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The tel: URI form, preferred by the RFC. An RFC 3986 URI in the canonical form of section 6.2.2 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.2.2>, e.g. "tel:+1-555-0100".
  uri String? @map("uri")

  /// The free-text form, for numbers that will not parse as a URI.
  text String? @map("text")

  /// TYPE parameter, section 5.6 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.6>.
  types String? @map("types")

  /// Telephone-specific TYPE values, section 6.4.1 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.4.1>.
  features String? @map("features")

  /// PREF parameter, section 5.3 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.3>.
  pref Int? @map("pref")

  /// Discriminator: which value oneof member is set (null = none).
  valueCase TelephoneValueCase? @map("value_case")

  /// Foreign key to Contact.
  contactID String @map("contact_id")

  /// Relation to Contact via contact_id.
  contact Contact @relation(fields: [contactID], references: [id], onDelete: Cascade)

  /// Index on [contact_id].
  @@index([contactID])
  /// Maps the model to the "telephones" table in the database.
  @@map("telephones")
  /// Maps the model to the "vcard" schema in the database.
  @@schema("vcard")
}

/// SocialProfile is the SOCIALPROFILE property, RFC 9554 section 3.5 <https://www.rfc-editor.org/rfc/rfc9554.html#section-3.5>. A presence on a social service. Section 3.5 gives the value type as URI by default but permits TEXT, because a handle is not always resolvable to a URI -- hence the oneof rather than a single string, which would have to guess which it received. Distinct from InstantMessage (IMPP, RFC 6350 section 6.4.3 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.4.3>): IMPP addresses a real-time messaging endpoint, this names a profile page or account. A service that is both gets both properties, which is what section 3.5 intends. Reference: RFC 9554 "vCard Format Extensions for JSContact", section 3.5. https://www.rfc-editor.org/rfc/rfc9554.html#section-3.5
model SocialProfile {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The profile URI, e.g. "https://example.com/@jane".
  uri String? @map("uri")

  /// A free-text handle, where no URI form exists.
  text String? @map("text")

  /// SERVICE-TYPE parameter, RFC 9554 section 4.9 <https://www.rfc-editor.org/rfc/rfc9554.html#section-4.9>. The service's name as it is presented to users, e.g. "Mastodon". Free text: section 4.9 registers no value list, because the set of services is open.
  serviceType String? @map("service_type")

  /// USERNAME parameter, RFC 9554 section 4.10 <https://www.rfc-editor.org/rfc/rfc9554.html#section-4.10>. The account name on that service, for when the URI does not contain it in a recoverable form.
  username String? @map("username")

  /// TYPE parameter, section 5.6 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.6>.
  types String? @map("types")

  /// PREF parameter, section 5.3 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.3>. 1 is most preferred, 100 least.
  pref Int? @map("pref")

  /// Discriminator: which value oneof member is set (null = none).
  valueCase SocialProfileValueCase? @map("value_case")

  /// Foreign key to Contact.
  contactID String @map("contact_id")

  /// Relation to Contact via contact_id.
  contact Contact @relation(fields: [contactID], references: [id], onDelete: Cascade)

  /// Index on [contact_id].
  @@index([contactID])
  /// Maps the model to the "social_profiles" table in the database.
  @@map("social_profiles")
  /// Maps the model to the "vcard" schema in the database.
  @@schema("vcard")
}
