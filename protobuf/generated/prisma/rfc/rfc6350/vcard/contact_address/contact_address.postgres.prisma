// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.4-0.20260906073202-9760202f187b+dirty
// 	protoc (unknown)
// source: protobuf/rfc6350/vcard/v1/contact_address.proto
//
// database: rfc
// schemas:  vcard
//
// annotations: entity.v1 v1.5.4-0.20260906073202-9760202f187b+dirty, store.v1 v1.5.4-0.20260906073202-9760202f187b+dirty
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

/// Address is the ADR property, RFC 6350 section 6.3.1 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.3.1>, as extended by RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. RFC 6350 fixed seven components in a defined order; RFC 9554 section 2.1 appends eleven more, so an address can name a room, a floor, a block or a landmark instead of flattening all of it into street_addresses. This is what makes addresses outside the seven-component Western model representable, and it is why google.type.PostalAddress was rejected for this package -- see docs/adding-data.md. Component order in the wire format is the order of the fields here. Reference: RFC 6350 "vCard Format Specification", section 6.3.1. https://www.rfc-editor.org/rfc/rfc6350.html#section-6.3.1 Reference: RFC 9554 "vCard Format Extensions for JSContact", section 2.1. https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1
model VcardAddress {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Post office box. Section 6.3.1 requires the component to exist in the structure but SHOULD be empty in new vCards; kept so a parsed vCard round-trips without silently dropping data.
  postOfficeBox String[] @map("post_office_box")

  /// Extended address. Same deprecation caveat as post_office_box.
  extendedAddresses String[] @map("extended_addresses")

  /// Street address.
  streetAddresses String[] @map("street_addresses")

  /// City or locality.
  localities String[] @map("localities")

  /// State, province or region.
  regions String[] @map("regions")

  /// Postal or ZIP code.
  postalCodes String[] @map("postal_codes")

  /// Country.
  countries String[] @map("countries")

  /// TYPE parameter, section 5.6 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.6>.
  types String? @map("types")

  /// PREF parameter, section 5.3 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.3>. 1 is most preferred, 100 least. Zero means unset, not most-preferred.
  pref Int? @map("pref")

  /// ROOM, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. Room, suite number or identifier.
  rooms String[] @map("rooms")

  /// APARTMENT, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. Apartment number, extension designation or box number.
  apartments String[] @map("apartments")

  /// FLOOR, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. Building floor or level.
  floors String[] @map("floors")

  /// NUMBER, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. Street number, which section 2.1 notes is not necessarily numeric.
  streetNumbers String[] @map("street_numbers")

  /// NAME, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. The street name on its own, without the number. Called `streets` rather than the RFC's "street name": AIP-122 reserves the `_name` suffix, and `street_addresses` above already holds the whole undivided line.
  streets String[] @map("streets")

  /// BUILDING, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. Building, tower or condominium.
  buildings String[] @map("buildings")

  /// BLOCK, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. Block name or number.
  blocks String[] @map("blocks")

  /// SUBDISTRICT, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. Subdistrict, ward or other administrative division below district.
  subdistricts String[] @map("subdistricts")

  /// DISTRICT, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. District or county.
  districts String[] @map("districts")

  /// LANDMARK, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. A nearby landmark or prominent feature, used for orientation where street numbering is sparse.
  landmarks String[] @map("landmarks")

  /// DIRECTION, RFC 9554 section 2.1 <https://www.rfc-editor.org/rfc/rfc9554.html#section-2.1>. Cardinal direction or quadrant, e.g. "NW".
  directions String[] @map("directions")

  /// LABEL parameter, RFC 9554 section 4.5 <https://www.rfc-editor.org/rfc/rfc9554.html#section-4.5>. The formatted address as it should be written on a mailing label, newlines included. Replaces RFC 6350 section 6.3.1's LABEL parameter with one that section 4.5 defines for general use.
  label String? @map("label")

  /// PHONETIC parameter, RFC 9554 section 4.6 <https://www.rfc-editor.org/rfc/rfc9554.html#section-4.6>. Names the notation the components are transcribed in, when they are a transcription.
  phonetic VcardPhoneticSystem? @map("phonetic")

  /// SCRIPT parameter, RFC 9554 section 4.8 <https://www.rfc-editor.org/rfc/rfc9554.html#section-4.8>. ISO 15924 four-letter script code. Required by section 4.6 when phonetic is PHONETIC_SYSTEM_SCRIPT.
  script String? @map("script")

  /// Foreign key to Contact.
  contactID String @map("contact_id")

  /// Relation to Contact via contact_id.
  contact Contact @relation(fields: [contactID], references: [id], onDelete: Cascade)

  /// Index on [contact_id].
  @@index([contactID])
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([postOfficeBox], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([extendedAddresses], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([streetAddresses], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([localities], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([regions], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([postalCodes], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([countries], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([rooms], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([apartments], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([floors], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([streetNumbers], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([streets], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([buildings], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([blocks], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([subdistricts], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([districts], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([landmarks], type: Gin)
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([directions], type: Gin)
  /// Maps the model to the "address" table in the database.
  @@map("address")
  /// Maps the model to the "vcard" schema in the database.
  @@schema("vcard")
}
