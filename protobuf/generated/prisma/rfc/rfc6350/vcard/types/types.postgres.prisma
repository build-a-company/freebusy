// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: protobuf/rfc6350/vcard/v1/types.proto
//
// database: rfc
// schemas:  vcard
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

/// Reference: RFC 6350 "vCard Format Specification", section 6.1.4. https://www.rfc-editor.org/rfc/rfc6350.html#section-6.1.4
enum VcardKind {
  /// A single person or entity.
  INDIVIDUAL

  /// A group of persons or entities.
  GROUP

  /// An organization.
  ORG

  /// A named geographical place.
  LOCATION

  /// Maps the enum to the "vcard_kind" type in the database.
  @@map("vcard_kind")
  /// Maps the enum to the "vcard" schema in the database.
  @@schema("vcard")
}

/// Organization is the ORG property, RFC 6350 section 6.6.4 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.6.4>. Semicolon-separated on the wire: the first component is the organization, the rest are progressively narrower units. Reference: RFC 6350 "vCard Format Specification", section 6.6.4. https://www.rfc-editor.org/rfc/rfc6350.html#section-6.6.4
model VcardOrganization {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The organization name itself, the first ORG component.
  value String @map("value")

  /// Organizational units, outermost first.
  units String[] @map("units")

  /// TYPE parameter, section 5.6 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5.6>.
  types String? @map("types")

  /// Foreign key to Contact.
  contactID String @map("contact_id")

  /// Relation to Contact via contact_id.
  contact Contact @relation(fields: [contactID], references: [id], onDelete: Cascade)

  /// Index on [contact_id].
  @@index([contactID])
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([units], type: Gin)
  /// Maps the model to the "organizations" table in the database.
  @@map("organizations")
  /// Maps the model to the "vcard" schema in the database.
  @@schema("vcard")
}

/// Kind is the KIND property, RFC 6350 section 6.1.4 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.1.4>. ExtensionProperty carries a vCard property this schema does not model. Not a hole punched in a typed schema: RFC 6350 section 6.10 <https://www.rfc-editor.org/rfc/rfc6350.html#section-6.10> defines exactly this, and section 3.3 <https://www.rfc-editor.org/rfc/rfc6350.html#section-3.3> types a property name as `iana-token / x-name`. Properties "with a name starting with X- may be defined bilaterally between two cooperating agents without outside registration", so a vCard that round-trips without them is lossy by construction. It is also what keeps the nine modelled properties honest. Anything the schema has not earned a typed field for survives here instead of forcing a speculative field table, and a property graduates out of this message when a consumer needs to query it. Reference: RFC 6350 "vCard Format Specification", section 6.10. https://www.rfc-editor.org/rfc/rfc6350.html#section-6.10
model VcardExtensionProperty {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The property name: an IANA-registered token or an X- name, e.g. "X-ABLabel" or "GEO". Not called `name`, which would make api-linter treat this message as a resource.
  key String @map("key")

  /// The property's values, one entry per comma-separated component. The schema cannot know the structure of a property it does not model, so these stay unparsed.
  values String[] @map("values")

  /// Property parameters, section 5 <https://www.rfc-editor.org/rfc/rfc6350.html#section-5>, e.g. {"TYPE": "work"}. Names are case-insensitive; values may not be.
  parameters Json? @map("parameters")

  /// Property group, section 3.3 <https://www.rfc-editor.org/rfc/rfc6350.html#section-3.3>. vCard allows `group.PROPERTY` to tie related lines together, which Apple's exporters rely on heavily.
  group String? @map("group")

  /// Foreign key to Contact.
  contactID String @map("contact_id")

  /// Relation to Contact via contact_id.
  contact Contact @relation(fields: [contactID], references: [id], onDelete: Cascade)

  /// Index on [contact_id].
  @@index([contactID])
  /// GIN index: tag containment (@>) cannot use a B-tree.
  @@index([values], type: Gin)
  /// Maps the model to the "extension_properties" table in the database.
  @@map("extension_properties")
  /// Maps the model to the "vcard" schema in the database.
  @@schema("vcard")
}
