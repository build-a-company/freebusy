// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.4
// 	protoc (unknown)
// source: protobuf/rfc7643/user/v1/types.proto
//
// database: rfc
// schemas:  user
//
// annotations: entity.v1 1.5.4, store.v1 1.5.4
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

/// Name is the User "name" attribute, RFC 7643 section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. Positional in the same way vCard's N is, and with the same limitation: five fixed slots plus a formatted string. SCIM predates RFC 9553's componentised model and has no equivalent, so a name that does not fit these slots loses structure here in a way it would not in a JSContact Card. Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.1.1. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1
model UserName {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The full name assembled for display, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>, including all prefixes and suffixes. SCIM calls this `formatted`; renamed because a bare adjective names no thing, and `display_name` is on AIP-122 <https://aip.dev/122>'s short allowlist for the `_name` suffix.
  displayName String? @map("display_name")

  /// Family name, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. SCIM: `familyName`.
  familyName String? @map("family_name")

  /// Given name, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. SCIM: `givenName`.
  givenName String? @map("given_name")

  /// Middle name, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. SCIM: `middleName`; renamed for AIP-122 <https://aip.dev/122>'s `_name` suffix rule, which permits only display_name, given_name and family_name.
  middleNames String? @map("middle_names")

  /// Honorific prefix, e.g. "Ms", section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>.
  honorificPrefix String? @map("honorific_prefix")

  /// Honorific suffix, e.g. "III", section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>.
  honorificSuffix String? @map("honorific_suffix")

  /// Back-relation: User records that reference this model via name_components_id.
  resource User[]

  /// Maps the model to the "names" table in the database.
  @@map("names")
  /// Maps the model to the "user" schema in the database.
  @@schema("user")
}

/// Attribute is SCIM's default multi-valued attribute, RFC 7643 section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. One shape for emails, phone numbers, IMs, photos, entitlements, roles and certificates alike -- section 2.4 defines `value`, `display`, `type` and `primary` once and every multi-valued attribute reuses them. That uniformity is deliberate in SCIM and is why this is a single message rather than one per attribute, which is the opposite of the choice RFC 9553 forced in the JSContact package. Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 2.4. https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4
model Attribute {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The significant value, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>: the email address, the phone number, the certificate.
  value String? @map("value")

  /// A human-readable label, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. Section 2.4 gives it a mutability of "immutable"; that is a server-side rule this schema states rather than enforces.
  display String? @map("display")

  /// What the value is for, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>, e.g. "work", "home", "mobile". Free text rather than an enum: section 2.4 defines the sub-attribute once, but each attribute that uses it declares its own canonical values -- "work"/"home"/"other" for emails, "work"/"home"/"mobile"/"fax"/"pager" for phone numbers -- and section 7 lets a provider add more. One enum could not be right for all of them, and one per attribute would defeat the point of section 2.4. Named `attribute_type`, not `type`: `type` is too close to the resource type in google.api.resource for a reader skimming both.
  attributeType String? @map("attribute_type")

  /// Whether this is the preferred value, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. At most one entry in a given attribute may set it; that is a cross-field rule and is not expressed as a buf.validate constraint, because no gate here checks CEL -- see docs/conventions.md rule 12.
  primary Boolean? @map("primary")

  /// A URI referencing the resource this value points at, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. SCIM spells this `$ref`, which is not a legal protobuf field name.
  referenceUri String? @map("reference_uri")

  /// Foreign key to User.
  userID String @map("user_id")

  /// Relation to User via user_id.
  user User @relation(fields: [userID], references: [id], onDelete: Cascade)

  /// Index on [user_id].
  @@index([userID])
  /// Maps the model to the "attributes" table in the database.
  @@map("attributes")
  /// Maps the model to the "user" schema in the database.
  @@schema("user")
}

/// Address is the User "addresses" attribute, RFC 7643 section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. Section 4.1.2 gives addresses their own sub-attributes rather than reusing Attribute's `value`, which is why this is a message of its own. Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.1.2. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2
model UserAddress {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The full address as it should be written, newlines included, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. SCIM calls this `formatted`.
  fullAddress String? @map("full_address")

  /// Street address, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. SCIM: `streetAddress`.
  streetAddress String? @map("street_address")

  /// City or locality, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>.
  locality String? @map("locality")

  /// State or region, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>.
  region String? @map("region")

  /// Postal or ZIP code, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. SCIM: `postalCode`.
  postalCode String? @map("postal_code")

  /// ISO 3166-1 alpha-2 code, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. SCIM calls this `country`; AIP-143 <https://aip.dev/143> requires `region_code` for an ISO 3166-1 value.
  regionCode String? @map("region_code")

  /// What the address is for, e.g. "work" or "home", section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>.
  attributeType String? @map("attribute_type")

  /// Whether this is the preferred address, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>.
  primary Boolean? @map("primary")

  /// Foreign key to User.
  userID String @map("user_id")

  /// Relation to User via user_id.
  user User @relation(fields: [userID], references: [id], onDelete: Cascade)

  /// Index on [user_id].
  @@index([userID])
  /// Maps the model to the "address" table in the database.
  @@map("address")
  /// Maps the model to the "user" schema in the database.
  @@schema("user")
}
