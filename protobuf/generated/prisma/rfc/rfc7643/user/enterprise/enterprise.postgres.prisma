// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.4-0.20260906073202-9760202f187b+dirty
// 	protoc (unknown)
// source: protobuf/rfc7643/user/v1/enterprise.proto
//
// database: rfc
// schemas:  user
//
// annotations: entity.v1 v1.5.4-0.20260906073202-9760202f187b+dirty, store.v1 v1.5.4-0.20260906073202-9760202f187b+dirty
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

/// EnterpriseUser is the Enterprise User schema extension, RFC 7643 section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. Identified in SCIM by the URN "urn:ietf:params:scim:schemas:extension:enterprise:2.0:User". A schema extension in SCIM is a namespaced block of extra attributes on a resource, which is a nested message here -- it has no identity of its own and no lifecycle apart from the User, so rule 8 makes it a value object. Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.3. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3
model EnterpriseUser {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// SCIM's `employeeNumber`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. The organization's own identifier for this person.
  employeeNumber String? @map("employee_number")

  /// SCIM's `costCenter`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>.
  costCenter String? @map("cost_center")

  /// SCIM's `organization`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. A string, not a reference to protobuf.rfc4519.organization.v1's Organization: AIP-215 <https://aip.dev/215> permits no cross-package message reference, and section 4.3 types it as free text rather than as a resource anyway.
  organization String? @map("organization")

  /// SCIM's `division`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>.
  division String? @map("division")

  /// SCIM's `department`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>.
  department String? @map("department")

  /// Foreign key to Manager.
  managerID String? @map("manager_id")

  /// Relation to Manager via manager_id.
  manager Manager? @relation(fields: [managerID], references: [id], onDelete: SetNull)

  /// Back-relation: User records that reference this model via enterprise_id.
  resource User[]

  /// Index on [manager_id].
  @@index([managerID])
  /// Maps the model to the "enterprise_users" table in the database.
  @@map("enterprise_users")
  /// Maps the model to the "user" schema in the database.
  @@schema("user")
}

/// Manager is the Enterprise User "manager" attribute, RFC 7643 section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.3. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3
model Manager {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The manager's SCIM `id`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>, which is the `uid` of another User in this collection. Not a google.api.resource_reference: that annotation names a resource *name* ("users/{user}") and section 4.3 types this as the bare id. The reference is real but the shape is the RFC's, not AIP's.
  value String? @map("value")

  /// A URI resolving to the manager's User resource, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. SCIM spells this `$ref`, which is not a legal protobuf field name.
  referenceUri String? @map("reference_uri")

  /// The manager's display name, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. Section 4.3 makes it read-only: it is a denormalised copy of the manager's own attribute, and writing it here would let the two disagree.
  displayName String? @map("display_name")

  /// Back-relation: EnterpriseUser records that reference this model via manager_id.
  enterpriseUsers EnterpriseUser[]

  /// Maps the model to the "managers" table in the database.
  @@map("managers")
  /// Maps the model to the "user" schema in the database.
  @@schema("user")
}
