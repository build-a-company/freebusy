// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store 1.5.3
// 	protoc (unknown)
// source: protobuf/rfc7643/user/v1/user.proto
//
// database: rfc
// schemas:  user
//
// annotations: entity.v1 1.5.3, store.v1 1.5.3
// engine:      protokit v1.3.1
//
// store — https://github.com/the-protobuf-project/store

/// User is the SCIM User resource, RFC 7643 section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. **The canonical identity model** under rule 14, holding the plain name that RFC 6350's vCard gave up when it became `Vcard`. protobuf.rfc4519.group.v1 and its LDAP siblings remain first-class packages with their own CRUD; this is what identity data is stored as. One caution that does not apply to the contact lineage: **there is no IETF-specified SCIM-to-LDAP conversion.** RFC 9555 maps vCard to JSContact element by element, so that codec follows a specification. Nothing equivalent exists here, so any mapping between this and RFC 4519 is a local decision and is labelled as one wherever it appears -- see rule 14 in docs/ontology.md. SCIM's own `id` (section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>) is the service provider's identifier, and this schema *is* the service provider, so it maps to `uid` rather than to a second field. `external_uid` is the client's identifier, which is the one that arrives from outside and must survive a round trip -- the same split rule 9 draws everywhere else. Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.1. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1
model User {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name, "users/{user}". Assigned by the server.
  name String @unique @map("name")

  /// Server-assigned unique id, SCIM's `id`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>.
  uid String? @unique @map("uid")

  /// SCIM's `externalId`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. The identifier the provisioning client uses for this user in its own system. Immutable and preserved verbatim: a provisioning client matches on it, so rewriting it would make every subsequent sync create a duplicate.
  externalUid String? @map("external_uid")

  /// SCIM's `userName`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. The login identifier, unique across the provider. REQUIRED by section 4.1. Named `username` rather than `user_name`: AIP-122 <https://aip.dev/122> bans the `_name` suffix outside a three-value allowlist this is not on.
  username String @map("username")

  /// SCIM's `displayName`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>.
  displayName String? @map("display_name")

  /// SCIM's `nickName`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>.
  nickname String? @map("nickname")

  /// SCIM's `profileUrl`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. A page describing the user.
  profileUri String? @map("profile_uri")

  /// SCIM's `title`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>, e.g. "Vice President".
  title String? @map("title")

  /// SCIM's `userType`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>, e.g. "Employee", "Contractor". Free text: section 4.1 gives canonical values but permits others.
  userType String? @map("user_type")

  /// SCIM's `preferredLanguage`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. A BCP 47 tag, RFC 5646 <https://www.rfc-editor.org/rfc/rfc5646.html>. Named per AIP-143 <https://aip.dev/143>.
  preferredLanguageCode String? @map("preferred_language_code")

  /// SCIM's `locale`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. How to format dates and numbers for this user -- distinct from `preferred_language_code`, which is what language to address them in.
  locale String? @map("locale")

  /// SCIM's `timezone`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. An IANA zone name, e.g. "Europe/London".
  timeZone String? @map("time_zone")

  /// SCIM's `active`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. Whether the account is in use. Deliberately not named `state` or `status`: AIP-216 <https://aip.dev/216> reserves both for a server-owned lifecycle, and this is administrative intent rather than lifecycle. It is also distinct from `delete_time` below -- a deactivated user still exists, which is the whole point of the attribute.
  active Boolean? @map("active")

  /// SCIM's `password`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. INPUT_ONLY: section 7.5 requires a service provider never to return it, and the field behaviour says so in the schema rather than leaving it to an implementer to remember.
  password String? @map("password")

  /// Weak validator for optimistic concurrency, AIP-154 <https://aip.dev/154>. This is SCIM's `meta.version`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>.
  etag String? @map("etag")

  /// When this user was soft-deleted, unset while it is live. AIP-164 <https://aip.dev/164>.
  deleteTime DateTime? @rfc.Timestamptz(6) @map("delete_time")

  /// When a soft-deleted user is purged for good. AIP-164 <https://aip.dev/164>.
  expireTime DateTime? @rfc.Timestamptz(6) @map("expire_time")

  /// When this user was created. SCIM's `meta.created`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>.
  createTime DateTime @default(now()) @rfc.Timestamptz(6) @map("create_time")

  /// When this user was last modified. SCIM's `meta.lastModified`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>.
  updateTime DateTime @updatedAt @rfc.Timestamptz(6) @map("update_time")

  /// Foreign key to Name.
  nameComponentsID String? @map("name_components_id")

  /// Relation to UserName via name_components_id.
  nameComponents UserName? @relation(fields: [nameComponentsID], references: [id], onDelete: SetNull)

  /// Foreign key to EnterpriseUser.
  enterpriseID String? @map("enterprise_id")

  /// Relation to EnterpriseUser via enterprise_id.
  enterprise EnterpriseUser? @relation(fields: [enterpriseID], references: [id], onDelete: SetNull)

  /// Back-relation: Attribute records that reference this model via user_id.
  attributes Attribute[]

  /// Back-relation: UserAddress records that reference this model via user_id.
  address UserAddress[]

  /// Index on [delete_time].
  @@index([deleteTime])
  /// Index on [name_components_id].
  @@index([nameComponentsID])
  /// Index on [enterprise_id].
  @@index([enterpriseID])
  /// Maps the model to the "resource" table in the database.
  @@map("resource")
  /// Maps the model to the "user" schema in the database.
  @@schema("user")
}
