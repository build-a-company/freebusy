// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store v1.5.1
// 	protoc (unknown)
// source: google/type/money.proto
//
// database: freebusy
// schemas:  common
//
// annotations: entity.v1 v1.5.1, store.v1 v1.5.1
// engine:      protokit v1.2.1
//
// store — https://github.com/the-protobuf-project/store

/// Represents an amount of money with its currency type.
model Money {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The three-letter currency code defined in ISO 4217.
  currencyCode String? @map("currency_code")

  /// The whole units of the amount. For example if `currencyCode` is `"USD"`, then 1 unit is one US dollar.
  units BigInt? @map("units")

  /// Number of nano (10^-9) units of the amount. The value must be between -999,999,999 and +999,999,999 inclusive. If `units` is positive, `nanos` must be positive or zero. If `units` is zero, `nanos` can be positive, zero, or negative. If `units` is negative, `nanos` must be negative or zero. For example $-1.75 is represented as `units`=-1 and `nanos`=-750,000,000.
  nanos Int? @map("nanos")

  /// Back-relation: RatePlan records that reference this model via price_id.
  ratePlans RatePlan[]

  /// Back-relation: PricingRateOverride records that reference this model via price_id.
  rateOverrides PricingRateOverride[]

  /// Back-relation: PricingLosDiscount records that reference this model via amount_off_id.
  losDiscounts PricingLosDiscount[]

  /// Back-relation: PricingFee records that reference this model via amount_id.
  fees PricingFee[]

  /// Back-relation: PriceBounds records that reference this model via floor_id.
  priceBounds PriceBounds[] @relation("PriceBoundsFloor")

  /// Back-relation: PriceBounds records that reference this model via ceiling_id.
  priceBounds2 PriceBounds[] @relation("PriceBoundsCeiling")

  /// Back-relation: Adjustment records that reference this model via amount_delta_id.
  adjustments Adjustment[]

  /// Back-relation: Redemption records that reference this model via amount_applied_id.
  redemptions Redemption[]

  /// Back-relation: Discount records that reference this model via amount_off_id.
  discounts Discount[]

  /// Back-relation: Scope records that reference this model via min_subtotal_id.
  scopes Scope[]

  /// Back-relation: Unit records that reference this model via price_id.
  units2 Unit[]

  /// Back-relation: PropertyRateOverride records that reference this model via price_id.
  rateOverrides2 PropertyRateOverride[]

  /// Back-relation: PropertyLosDiscount records that reference this model via amount_off_id.
  losDiscounts2 PropertyLosDiscount[]

  /// Back-relation: PropertyFee records that reference this model via amount_id.
  fees2 PropertyFee[]

  /// Back-relation: Booking records that reference this model via price_id.
  bookings Booking[] @relation("BookingPrice")

  /// Back-relation: Booking records that reference this model via discount_id.
  bookings2 Booking[] @relation("BookingDiscount")

  /// Back-relation: Booking records that reference this model via total_id.
  bookings3 Booking[] @relation("BookingTotal")

  /// Back-relation: Booking records that reference this model via refund_amount_id.
  bookings4 Booking[] @relation("BookingRefundAmount")

  /// Back-relation: PriceComponent records that reference this model via amount_id.
  priceComponents PriceComponent[]

  /// Maps the model to the "moneys" table in the database.
  @@map("moneys")
  /// Maps the model to the "common" schema in the database.
  @@schema("common")
}
